{
  "name": "alfanumrik-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/host",
    "build": "npm run build -w apps/host",
    "vercel-build": "npm run build -w apps/host && node -e \"require('fs').cpSync('apps/host/.next', '.next', {recursive: true})\""
  }
}
