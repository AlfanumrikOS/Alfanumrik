{
  "name": "alfanumrik-monorepo",
  "private": true,
  "scripts": {
    "dev": "cd alfanumrik-frontend && npm install && npm run dev",
    "build": "cd alfanumrik-frontend && npm install && npm run build"
  }
}
