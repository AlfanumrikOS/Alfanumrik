# Product Readiness Execution Ledger — Backend RCA

Date: 2026-07-09
Source RCA: `engineering-audit/FULL_RCA_BACKEND_WORKFLOWS_PRODUCT_READINESS_2026-07-09.md`

## Executed In This Pass

| RCA item | Execution | Evidence |
|---|---|---|
| RCA-16 school-admin roster CSV parsing | Replaced split-based CSV parsing in `apps/host/src/app/api/school-admin/students/route.ts` with a quote-aware parser that supports quoted commas, escaped quotes, CRLF, and quoted newlines. | Added regression in `apps/host/src/__tests__/api/school-admin-students-post.test.ts`; focused suite passes. |
| RCA-01 / RCA-02 admin-client guard clarity | Corrected the admin-client allowlist test title to match the current frozen count of 269 routes. | `apps/host/src/__tests__/api-admin-client-allowlist.test.ts` now matches `EXPECTED_COUNT = 269`. |
| RCA-02 route access metadata contract | Added `scripts/route-access-manifest.json` declaring an access category, owner, rationale, and service-role justification where applicable for all 363 API route handlers; added `scripts/gen-route-access-manifest.mjs` to regenerate the artifact. | Added `apps/host/src/__tests__/api/route-access-manifest.test.ts`; it fails if a route is missing, uses an invalid access category, or imports the service-role/admin client without a manifest justification. |
| RCA-17 Vercel cron job registry | Added `scripts/job-registry.json` with owner, schedule, idempotency key, last-success metric, and alert threshold for every Vercel cron. | Added `apps/host/src/__tests__/api/cron-job-registry.test.ts`; it fails if `vercel.json` and the registry diverge. |
| RCA-07 OpenAPI check execution | Repaired the workspace `gen:openapi` script path and generator import after the check exposed broken paths; regenerated `openapi/v2.json` from the current package contract. | `npm run gen:openapi:check -w apps/host` passes. |
| RCA-15 public API v1 contract coverage | Added the live `/api/public/v1/openapi` endpoint to `docs/public-api/openapi.json` and pinned the canonical spec to the actual public v1 route surface. | Added `apps/host/src/__tests__/public-api/openapi-route.test.ts`; it fails if a public v1 route file is missing from the canonical public API spec. |
| RCA-08 Edge function deploy/secrets manifest | Added `scripts/edge-function-manifest.json` covering every active `supabase/functions/*/index.ts` function with deploy flag, owner, class, health-check handle, and required direct `Deno.env.get(...)` secrets. | Added `apps/host/src/__tests__/edge-functions/edge-function-manifest.test.ts`; it fails on missing functions or undeclared direct env reads. |
| RCA-22 legacy quiz/API inventory | Added `scripts/legacy-api-inventory.json` covering legacy v1 quiz RPC wrappers and the client-direct v2 RPC submit path, including owner, risk, status, deprecation condition, planned action, and evidence. | Added `apps/host/src/__tests__/api/legacy-api-inventory.test.ts`; it fails if required legacy quiz surfaces lose inventory/deprecation metadata. |
| RCA-19 / RCA-20 tenant isolation and pre-rollout gates | Repaired the host workspace tenant-isolation scripts, fixed the static tenant audit root to the current host API tree, regenerated the 362-route tenant baseline, and repaired the pre-rollout verifier for shared package paths and the current student quiz route. | `npm run eval:tenant-isolation -w apps/host` now scans and gates 362 routes; `npx tsx scripts/pre-rollout-checklist.ts` passes 14/14; `apps/host/src/__tests__/eval/tenant-isolation-evaluator.test.ts` now fails on zero-route scans. |
| RCA-13 grounded-answer config parity | Aligned `packages/lib/src/grounding-config.ts` with the Deno grounded-answer prompt registry by adding the mode-specific Foxy prompt template IDs. | `npx tsx scripts/pre-rollout-checklist.ts` and `apps/host/src/__tests__/grounding/config-parity.test.ts` pass against shared web config and Edge function config. |
| RCA-13 Foxy/RAG safety and streaming readiness manifest | Added `scripts/foxy-rag-readiness.json` tying the grounded output backstop, streaming contract, streaming first-paint output backstop, JSON fallback abstain behavior, grounded config parity, workflow status, and backlog status into one product-readiness artifact. | Added `apps/host/src/__tests__/foxy-rag-readiness.test.ts`; it fails if the manifest loses required artifacts, evidence snippets, or reintroduces the FOX-7/streaming-residual/Hindi follow-up trail. |
| RCA-24 feature flag environment matrix | Added `scripts/feature-flag-matrix.json` declaring default, staging, and production intent for every registered `FLAG_DEFAULTS` flag plus reviewed live rollout overrides from `scripts/feature-flag-matrix.overrides.json`; updated `scripts/gen-feature-flag-matrix.mjs` to merge the override artifact without losing default coverage. | Added/updated `apps/host/src/__tests__/lib/feature-flag-matrix.test.ts`; it fails if a registered default flag is missing, if override-sourced entries lack owner/rationale/source, or if production-ON intent lacks evidence. |
| RCA-18 DB function hardening | Added `supabase/migrations/20260707020000_rca18_db_function_execute_grants.sql` to revoke default `PUBLIC` execute and explicitly grant `authenticated, service_role` for the high-risk SECURITY DEFINER functions redefined by the July RCA migration: `submit_quiz_results`, `submit_quiz_results_v2`, and `match_rag_chunks_ncert`. Added `scripts/db-function-hardening.json` to record owner, risk, definition migration, grant migration, allowed roles, and ownership-check posture. | Added `apps/host/src/__tests__/db-function-hardening.test.ts`; it fails if the manifest is missing, if the functions lose SECURITY DEFINER/search_path pins, if quiz RPC ownership checks disappear, or if the grant migration no longer revokes `PUBLIC` and grants only the explicit allowed roles. |
| RCA-23 incident ID propagation | Preserved a single `x-request-id` through the host proxy by forwarding it to downstream handlers and mirroring it as `X-Request-Id` on the response. Added `scripts/incident-id-spine.json` documenting the current host proxy -> `board-score` host route -> Supabase Edge Function -> shared Edge audit log -> super-admin related-event lookup chain. | Added `apps/host/src/__tests__/incident-id-propagation.test.ts`; it fails if the proxy regenerates a divergent response ID, stops forwarding `x-request-id`, or if the incident spine manifest loses required cross-surface evidence. |
| RCA-12 route/page/flag/API matrix | Added `scripts/product-surface-matrix.json` for seven high-demo-risk product surfaces: Today home, quiz submit, adaptive tutor, parent glance, teacher command center, school-admin roster, and school pulse. Each row maps audience, page, primary CTA, backend APIs, feature flags, DB-write posture, tests, docs, readiness, and owner. Centralized the route-local tutor flags (`ff_tutor_v1`, `ff_tutor_bkt_v1`) in the flag registry/defaults and regenerated the 58-flag environment matrix. | Added `apps/host/src/__tests__/product-surface-matrix.test.ts`; it fails if a core surface is missing, points at non-existent page/API/test/doc files, or references a feature flag that is not registered in `FLAG_DEFAULTS`. |
| RCA-10 AO-3 provisioning model inventory | Added `scripts/provisioning-path-inventory.json` covering the active super-admin, public claim/trial, and school-admin adjacent provisioning paths with auth gates, canonical helpers, write targets, risks, status, and AO-3 follow-up notes. | Added `apps/host/src/__tests__/api/provisioning-path-inventory.test.ts`; it fails if required provisioning paths are missing, point at non-existent routes, or cite evidence that no longer exists in the route source. |
| RCA-25 mobile /v2 contract parity | Added `scripts/mobile-v2-contract-manifest.json` mapping every OpenAPI `/v2` operation to its host route and generated Dart client method, including owner/surface/status metadata. | Added `apps/host/src/__tests__/mobile-v2-contract-manifest.test.ts`; it fails if an OpenAPI `/v2` operation is missing from the mobile manifest, if the host route is gone, or if the generated Dart API no longer exposes the pinned method/path. |
| RCA-11 AO-10b grade normalization readiness | Added `scripts/grade-normalization-readiness.json` tying the AO-10b backfill migration, source-shape pin, live verifier, and read-time normalization path into one product-readiness artifact. | Added `apps/host/src/__tests__/grade-normalization-readiness.test.ts`; it fails if the RCA-11 artifact loses migration/test/verifier/read-path coverage or cites evidence that no longer exists. |
| RCA-14 super-admin PII export readiness | Added `scripts/super-admin-pii-readiness.json` consolidating the CEO-approved `super_admin` tiering for PII report exports, UUID-only support-floor exceptions, observability CSV egress redaction, regression pins, shared redactor barrel, and lower-tier staff notification follow-up. | Added `apps/host/src/__tests__/super-admin-pii-readiness.test.ts`; it fails if the RCA-14 readiness artifact loses route/test/redactor/backlog evidence or drops the explicit ops notification state. |
| RCA-20 certification E2E readiness manifest | Added `scripts/certification-readiness-manifest.json` tying the seven-role `e2e/certification` suite, Playwright collection gate, payment second gate, certification seed/teardown scripts, traceability runbook, rollback runbook, and risk-register blockers into one launch-readiness artifact. | Added `apps/host/src/__tests__/certification-readiness-manifest.test.ts`; it fails if a mission-role spec disappears, loses its certification gate/evidence, or if CERT-17/CERT-FE-01/CERT-07 are dropped from the live-blocker trail. |
| RCA-04 / RCA-06 student-learning readiness manifest | Added `scripts/student-learning-readiness.json` tying SLC-1 single-XP-writer, SLC-4 fallback daily-cap alignment, SLC-5 anti-cheat advisory convergence, canonical quiz submit route `p_session_id` wiring, v2 quiz submit route `p_session_id` wiring, pre-rollout `shuffle_map` checks, and workflow residual follow-ups into one readiness artifact. | Added `apps/host/src/__tests__/student-learning-readiness.test.ts`; it fails if SLC-1/SLC-4/SLC-5 evidence disappears, if the submit routes stop pinning session IDs to the atomic writer, or if the SLC-1-backfill/SLC-5/SLC-8 follow-up trail is dropped. |
| RCA-01 / XC-3 high-sensitivity service-role migration batch | Added `scripts/xc3-service-role-migration-batch.json` to turn the flat 269-route service-role allowlist into a prioritized first batch: parent child export, parent erasure, parent report, school-admin roster, teacher class join, super-admin reports as reviewed exception, and parent child chat as the RLS-scoped pattern to copy. | Added `apps/host/src/__tests__/xc3-service-role-migration-batch.test.ts`; it fails if batch routes disappear from the route-access manifest, if migrate-first entries stop matching live service-role usage, or if the RLS-scoped exemplar regresses to `supabase-admin`. |
| RCA-03 / RCA-21 TSB-4 canonical membership cutover readiness | Added `scripts/tsb4-canonical-membership-cutover.json` to turn the deferred class membership consolidation into an ordered execution plan: landed soft-delete sync, landed teacher RLS/fail-closed backfill, divergence quantification, boundary-reader repoint, route-helper repoint, live tenant smoke, and CEO-gated legacy-table retirement. | Added `apps/host/src/__tests__/tsb4-canonical-membership-cutover-readiness.test.ts`; it fails if the cutover manifest is missing, loses the canonical/legacy table posture, drops the required stage order, or cites artifacts/evidence that no longer exist. |
| RCA-06 SLC-1 historical XP quantification | Added `scripts/historical-xp-inflation-quantification.sql`, a read-only target-DB query that reports daily quiz XP over-cap rows, duplicate quiz ledger reference IDs, cached `students.xp_total` vs ledger deltas, quiz-session-vs-ledger deltas, impacted-student totals, and a leaderboard-risk sample before any clamp/backfill decision. Updated `scripts/student-learning-readiness.json` to include this as an `operator_gate`. | Added `apps/host/src/__tests__/historical-xp-inflation-quantification.test.ts`; it fails if the SQL artifact is missing, contains mutating SQL, drops the required RCA-06 metrics, or is no longer linked from the student-learning readiness manifest. |
| RCA-24 live feature-flag DB comparison | Added `scripts/verify-feature-flag-matrix.ts`, a read-only verifier that loads `scripts/feature-flag-matrix.json`, reads live `feature_flags` rows, and fails on missing registered flags, staging/production enabled-state drift, zero-rollout drift for intended-ON flags, or unexpected active live `ff_*` rows. Classified the live AI/AlfaBot/grounded/rollout flags into the override matrix instead of disabling them, and inserted the five missing default-OFF rows: `ff_cosmic_redesign_v1`, `ff_practice_os_v1`, `ff_revision_os_v1`, `ff_subjects_os_v1`, `ff_test_os_v1`. | Added/updated `apps/host/src/__tests__/feature-flag-live-matrix-verifier.test.ts`; live `npx tsx scripts/verify-feature-flag-matrix.ts --env=production` and `--env=staging` now both exit 0 with `checked: 120`, no missing, no mismatched, and no unexpected flags. |
| RCA-08 / RCA-09 Edge shared-secret activation | Added `scripts/edge-secret-activation-readiness.json` to capture the shared-module Upstash secret dependency that direct `index.ts` scans cannot see, and updated `parent-portal` in `scripts/edge-function-manifest.json` to require `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN`. | Added `apps/host/src/__tests__/edge-functions/edge-secret-activation-readiness.test.ts`; it fails if `parent-portal` loses the Upstash secret declarations, if the shared-secret activation artifact disappears, or if the evidence trail from `_shared/durable-rate-limiter.ts` to `parent-portal` breaks. |
| RCA-19 / RCA-20 live tenant isolation smoke | Added `scripts/verify-live-tenant-isolation-smoke.ts`, an operator-run verifier for seeded staging/live tenant fixtures. It probes parent, teacher, and school-admin cross-tenant reads using only bearer role tokens, expects denial or sanitized output, and fails if a response leaks the seeded tenant-B markers. | Added `apps/host/src/__tests__/live-tenant-isolation-smoke.test.ts`; it validates fixture-env requirements, request construction, denial/sanitization semantics, and bearer/no-cache execution without service-role credentials. |
| RCA-18 live DB function grant inspection | Added `scripts/verify-db-function-hardening-live.ts`, a dependency-free verifier that prints the read-only `pg_catalog` query for the RCA-18 manifest functions and compares target-DB JSON rows against expected `SECURITY DEFINER`, public-pinned `search_path`, `PUBLIC` execute revoked, and `authenticated/service_role` execute grants. Executed the linked target DB export via `supabase db query --linked`; applied exact grant hardening for `submit_quiz_results`, `submit_quiz_results_v2`, and `match_rag_chunks_ncert`. | Added/updated `apps/host/src/__tests__/db-function-live-grant-verifier.test.ts`; `npx tsx scripts/verify-db-function-hardening-live.ts --input=artifacts/rca18-db-function-grants.json` now passes 3/3 functions. |
| RCA-17 live job health verification | Added `scripts/verify-job-health-live.ts`, a dependency-free verifier that prints a read-only `ops_events` export query for every `scripts/job-registry.json` last-success metric and compares exported rows against each registry alert threshold. | Added `apps/host/src/__tests__/job-health-live-verifier.test.ts`; it validates alert-threshold parsing and missing/stale live metric failures. |
| RCA-14 lower-tier PII exporter notification | Added `docs/runbooks/super-admin-pii-export-notification.md`, with the lower-tier exporter notification template, post-deploy `admin_audit_log` review query, and no-loosening guard for the four PII report types. Linked it from `scripts/super-admin-pii-readiness.json`. | Added `apps/host/src/__tests__/super-admin-pii-ops-runbook.test.ts`; it fails if the runbook disappears, drops notification/audit/no-loosening evidence, or is no longer linked from the RCA-14 readiness manifest. |
| RCA-23 live incident-ID observability proof | Added `scripts/verify-incident-id-live.ts`, a dependency-free verifier that prints a read-only `ops_events` query for a sampled `X-Request-Id` and compares exported event rows against the response request ID and optional required sources. | Added `apps/host/src/__tests__/incident-id-live-verifier.test.ts`; it validates missing response IDs, absent event rows, and missing required observability sources. |
| RCA-04 / RCA-22 / RCA-25 mobile legacy traffic cutover proof | Added `scripts/verify-mobile-legacy-traffic-live.ts`, a dependency-free verifier that prints a read-only `api_request_logs` export query and compares mobile release telemetry against the RCA-22 legacy quiz inventory plus explicitly blocked legacy payment paths. | Added `apps/host/src/__tests__/mobile-legacy-traffic-verifier.test.ts`; it fails if mobile traffic still hits legacy quiz RPC/direct-submit surfaces or old payment endpoints instead of `/api/v2/quiz/submit` and `/api/payments/create-order` + `/api/payments/verify`. |
| RCA-20 product-readiness release gate runner | Added `scripts/product-readiness-release-gate.ts`, a single repo-owned release-gate runner that executes the type-check, lint, focused RCA regression suites, tenant-isolation eval, pre-rollout checklist, build, and OpenAPI drift check in order. It runs host-scoped Vitest gates from `apps/host` while keeping workspace gates at repo root, and lists live/operator gates separately so local green checks cannot be mistaken for broad-launch proof. | Added `apps/host/src/__tests__/product-readiness-release-gate.test.ts`; it fails if the gate drops key repo commands, mixes operator-owned live proof into local execution, loses dry-run safety, loses host-gate working-directory metadata, or stops on failure incorrectly. Full repo-owned gate passed on 2026-07-10 with `npx tsx scripts/product-readiness-release-gate.ts`: 39/39 repo gates passed. |
| RCA-20 live/operator evidence bundle contract | Added `scripts/live-readiness-evidence-manifest.json` and `scripts/verify-live-readiness-evidence.ts` to define the broad-launch evidence required for every operator-owned gate listed by the release runner. The verifier can print an operator-fillable bundle template and fails on missing gates, failed/not-run status, stale evidence, missing evidence files, or unapproved accepted-risk decisions. | Added `apps/host/src/__tests__/live-readiness-evidence.test.ts`; it fails if the manifest drifts from `scripts/product-readiness-release-gate.ts` operator gates, if template generation drops a gate/command/approval placeholder, or if evidence-bundle comparison stops enforcing pass/approval/freshness/path checks. |
| RCA-11 live grade-format verifier reliability | Reworked `scripts/verify-grade-format.ts` to return an exit code and set `process.exitCode` instead of calling `process.exit(...)`, preventing the Windows libuv assertion that occurred after successful Supabase reads. | Added `apps/host/src/__tests__/grade-format-verifier.test.ts`; live `npx tsx scripts/verify-grade-format.ts` exits 0 and confirms `question_bank` and `curriculum_topics` grades conform to P5. |
| RCA-24 feature-flag drift execution planner | Added `scripts/reconcile-feature-flag-matrix.ts`, a dry-run-first reconciliation planner with single-environment and full-matrix planning. It supports `--env=all` and uses update-then-insert rather than assuming an upsert constraint. | Added `apps/host/src/__tests__/feature-flag-reconciliation.test.ts`; focused verifier tests pass. The production/staging read-only verifier is the broad-launch gate, and it is now green against the live target. |
| RCA-01 / XC-3 parent event-bus publisher migration | Removed the remaining route-level service-role dependency from `/api/parent/children/[student_id]/export` and `/api/parent/children/[student_id]/request-erasure` by moving parent child-data `state_events` publishing behind `public.parent_publish_child_state_event(...)`. The helper validates `auth.uid()` against active/approved guardian-child ownership, checks tenant binding, preserves `ff_event_bus_v1`, restricts event kinds to the three parent child-data events, and inserts idempotently with `ON CONFLICT (idempotency_key) DO NOTHING`. The admin-client allowlist ratcheted from 267 to 265 routes. | Added `supabase/migrations/20260710080000_xc3_parent_child_state_event_rpc.sql` and `apps/host/src/__tests__/parent-child-state-event-rpc-migration.test.ts`; updated parent export/erasure route tests, route-access manifest, `scripts/admin-client-allowlist.json`, and `scripts/xc3-service-role-migration-batch.json`. Focused red-green slice passed 5 files / 34 tests; manifest/allowlist guards passed 6 files / 13 tests. Applied the helper to linked project `shktyoxqhundlvkiwguu`; linked evidence `artifacts/xc3-parent-child-state-event-rpc-live.json` shows it is `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `public`. |
| RCA-01 / XC-3 school-admin POST attach migration | Kept the unavoidable Auth Admin user creation exception in `/api/school-admin/students`, but moved the post-auth `students.school_id`/phone attach and optional legacy class roster insert behind `public.school_admin_attach_created_student(...)`. The helper resolves the caller's school from `auth.uid()`, rejects cross-school `class_id`, updates only a trigger-created student row that is unassigned or already same-school, and inserts same-school `class_students` idempotently. | Added `supabase/migrations/20260710090000_xc3_school_admin_student_attach_rpc.sql`; updated `apps/host/src/__tests__/school-admin-students-rpc-migration.test.ts`, `apps/host/src/__tests__/api/school-admin-students-post.test.ts`, and `scripts/xc3-service-role-migration-batch.json`. Red run failed on the old direct attach path; focused green run passed 2 files / 15 tests. Applied the helper to linked project `shktyoxqhundlvkiwguu`; linked evidence `artifacts/xc3-school-admin-student-attach-rpc-live.json` shows it is `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `public`. |
| RCA-01 / XC-3 school-admin POST preflight migration | Moved school-admin student-create duplicate-email checks and legacy `seats_purchased` preflight reads behind `public.school_admin_student_create_preflight(text, integer)`. The helper resolves the caller's school from `auth.uid()`, checks duplicate student email, returns active-seat usage and purchased seats for the legacy OFF path, and reports seat-cap violations without route-level `students`/`school_subscriptions` reads. The route still keeps service-role for Auth Admin user creation, class ownership precheck, and import orchestration, so this remains a partial migration. | Added `supabase/migrations/20260710100000_xc3_school_admin_student_create_preflight_rpc.sql`; updated `apps/host/src/app/api/school-admin/students/route.ts`, `apps/host/src/__tests__/school-admin-students-rpc-migration.test.ts`, `apps/host/src/__tests__/api/school-admin-students-post.test.ts`, and `scripts/xc3-service-role-migration-batch.json`. Red run failed on missing preflight RPC/direct route reads; focused green run passed 2 files / 18 tests. Applied the helper to linked project `shktyoxqhundlvkiwguu`; linked evidence `artifacts/xc3-school-admin-student-create-preflight-rpc-live.json` shows it is `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `public`. |
| RCA-01 / XC-3 school-admin POST class preflight migration | Removed the remaining route-level service-role `classes` ownership precheck from `/api/school-admin/students` single-create. The authenticated preflight RPC now accepts optional `p_class_id` and rejects missing/cross-school classes under `auth.uid()` before Auth Admin user creation, avoiding orphan Auth users and keeping class ownership checks inside the scoped DB helper. | Added `supabase/migrations/20260710110000_xc3_school_admin_student_create_class_preflight_rpc.sql`; updated `apps/host/src/app/api/school-admin/students/route.ts`, `apps/host/src/__tests__/school-admin-students-rpc-migration.test.ts`, `apps/host/src/__tests__/api/school-admin-students-post.test.ts`, and `scripts/xc3-service-role-migration-batch.json`. Red runs failed on the old `.from('classes')` read, missing `p_class_id`, and missing migration; focused green run passed 2 files / 20 tests. Applied the helper to linked project `shktyoxqhundlvkiwguu` with `supabase db query --linked -f supabase/migrations/20260710110000_xc3_school_admin_student_create_class_preflight_rpc.sql`. |
| RCA-01 / XC-3 school-admin Auth Admin narrowing | Removed the broad `@alfanumrik/lib/supabase-admin` import from `/api/school-admin/students` and isolated the unavoidable Supabase Auth Admin `createUser` call behind `@alfanumrik/lib/school-admin/student-auth-admin`. The route now uses RLS-scoped request clients/RPCs for roster, preflight, class validation, and post-auth attachment, and no longer carries a route-level service-role DB client exception. | Added `packages/lib/src/school-admin/student-auth-admin.ts`; updated `apps/host/src/app/api/school-admin/students/route.ts`, `scripts/admin-client-allowlist.json`, `scripts/route-access-manifest.json`, `scripts/xc3-service-role-migration-batch.json`, and the XC-3/admin-client guards. Red runs failed on the old route import and 265-count allowlist; green runs ratcheted the route-level admin-client allowlist from 265 to 264 and passed the school-admin/XC-3 cluster. |
| RCA-13 Foxy/RAG Hindi output-screen coverage | Closed the minor Hindi/Devanagari profanity-token follow-up by adding narrow Hindi/Hinglish abusive-token patterns to both the Node and Deno `screenStudentFacingText` twins, while preserving legitimate Hindi curriculum text. | Added red-green coverage in `apps/host/src/__tests__/lib/ai/validation/output-screen.test.ts` and parity coverage in `apps/host/src/__tests__/lib/ai/validation/output-screen-deno-parity.test.ts`; updated `scripts/foxy-rag-readiness.json` so Hindi-token coverage is now a `repo_guarded` artifact instead of an open follow-up. |
| RCA-13 / FOX-7 legacy fallback output backstop | Extended the deterministic `screenStudentFacingText` backstop to the Foxy legacy intent-router fallback persist path, covering both the `ff_grounded_ai_foxy` kill-switch path and the grounded-service fallback path. Unsafe legacy text now returns the safe hard-abstain envelope, refunds quota, emits category-only telemetry/audit, and is not written to `foxy_chat_messages`. | Added red-green coverage in `apps/host/src/__tests__/api/foxy/output-safety-backstop.test.ts`; updated `scripts/foxy-rag-readiness.json` so FOX-7 is now a `repo_guarded` artifact instead of an open follow-up. |
| RCA-13 streaming first-paint output backstop | Closed the streaming residual by buffering student-visible `text`/`done` SSE frames inside `/api/foxy` until the complete answer passes `screenStudentFacingText`. Safe streams release buffered text/done at close; unsafe streams emit only the safe hard-abstain frame, refund quota, persist empty content, and never put unsafe deltas on the wire. | Added red-green coverage in `apps/host/src/__tests__/api/foxy/streaming-structured-persistence.test.ts`; updated `scripts/foxy-rag-readiness.json` so the streaming first-paint backstop is a `repo_guarded` artifact and `remainingFollowUps` is empty. |
| RCA-15 localhost public OpenAPI endpoint | Fixed `/api/public/v1/openapi` to resolve `docs/public-api/openapi.json` from either repo-root cwd or `apps/host` cwd. The localhost Next server was returning 500 because it looked for `apps/host/docs/public-api/openapi.json`. | Extended `apps/host/src/__tests__/public-api/openapi-route.test.ts`; localhost `GET http://localhost:3000/api/public/v1/openapi` now returns 200. |
| RCA-17 cron job-health emission source | Added `packages/lib/src/cron-job-health.ts`, an awaited service-role writer for `ops_events.category = 'job_health'`, and wired every `scripts/job-registry.json` Vercel cron route to emit its registered `lastSuccessMetric` after a successful run or benign skip. | Added `apps/host/src/__tests__/api/cron-job-health-instrumentation.test.ts`; focused Vitest and host TypeScript pass. Live verifier still waits for real scheduler executions. |
| RCA-17 localhost cron job-health smoke | Added `/api/internal/cron/job-health-smoke`, an internal CRON_SECRET-gated smoke route that records a dedicated `ops.cron.job_health_smoke.last_success_at` job-health row and returns 500 if the shared writer fails. The shared writer now reports success/failure to callers while preserving existing cron behavior. | Added `apps/host/src/__tests__/api/cron/job-health-smoke.test.ts`; updated `scripts/product-readiness-release-gate.ts` and route-access/tenant-isolation guards. Localhost production smoke on port 3017 returned 401 without a secret and 200 with `x-cron-secret`, proving the auth gate and DB write path. This does not replace the real scheduler 13-job live metric gate. |
| RCA-04 / RCA-22 / RCA-25 mobile traffic telemetry source | Added `public.api_request_logs` with RLS no-client-access policy and service-role-only writes, plus lightweight host proxy telemetry for API requests including path, RPC name, client platform, request ID, method, and truncated user-agent. The mobile verifier now accepts Supabase CLI `{ rows: [...] }` exports. Applied the migration to linked project `shktyoxqhundlvkiwguu`. | Added `apps/host/src/__tests__/mobile-request-log-instrumentation.test.ts` and extended `apps/host/src/__tests__/mobile-legacy-traffic-verifier.test.ts`; `npx tsx scripts/verify-mobile-legacy-traffic-live.ts --input=artifacts/rca04-mobile-legacy-traffic.json` passes with 0 linked telemetry rows and no legacy mobile quiz/payment traffic. |
| RCA-03 / RCA-21 TSB-4 canonical membership boundary repoint | Repointed the central teacher student boundary in `packages/lib/src/rbac.ts:canAccessStudent` and the teacher route-local roster checks in `/api/teacher/remediation` and `/api/teacher/parent-notify` from legacy `class_students` to canonical `class_enrollments`. Added `scripts/tsb4-class-membership-divergence-quantification.sql` and ran it against the linked DB. | Updated/added TSB tests: `tsb4-enrollments-rls-reconcile`, `tsb4-canonical-membership-cutover-readiness`, `security/tenant-isolation-role-scoped-apis`, `api/teacher/active-enrollment-scoping`, `api/teacher/remediation/route`, `api/teacher/parent-notify/route`, and `tsb4-divergence-quantification`; focused run passed 7 files / 80 tests, plus live divergence export `artifacts/tsb4-class-membership-divergence.json` showed only `matched_or_both_inactive` with `pair_count = 18`. |
| RCA-01 / XC-3 teacher join-class service-role migration | Replaced route-level `supabase-admin` usage in `/api/teacher/join-class` with an RLS-scoped anon-key Supabase client calling the authenticated `public.teacher_join_class_by_code(text)` helper. The helper binds the join to `auth.uid()`, active non-deleted class code lookup, idempotent `class_teachers` insert, and guarded teacher `school_id` adoption. Applied the helper to linked project `shktyoxqhundlvkiwguu`. | Added `supabase/migrations/20260710020000_xc3_teacher_join_class_rpc.sql`, `apps/host/src/__tests__/teacher-join-class-rpc-migration.test.ts`, updated `apps/host/src/__tests__/api/track-b/teacher-join-class.test.ts`, `scripts/xc3-service-role-migration-batch.json`, and `scripts/route-access-manifest.json`. Focused run passed 4 files / 14 tests; linked catalog evidence `artifacts/xc3-teacher-join-class-rpc-live.json` shows `security_definer = true`, `search_path=public`, `authenticated_execute = true`, `anon_execute = false`, and `public_execute = false`. |
| RCA-01 / XC-3 parent report cache service-role migration | Replaced route-level `supabase-admin` cache reads/upserts in `/api/parent/report` with an RLS-scoped request client. Added a corrective linked-target RLS migration because live `parent_weekly_reports` policies had drifted to student-owned `own_*` policies; the route now relies on guardian-link policies via `public.is_guardian_of(student_id)`. Ratcheted `scripts/admin-client-allowlist.json` from 269 to 267 after migrating `/api/teacher/join-class` and `/api/parent/report`. | Added `supabase/migrations/20260710030000_xc3_parent_report_cache_guardian_rls.sql` and `apps/host/src/__tests__/parent-report-cache-rls-corrective.test.ts`; updated `apps/host/src/__tests__/api/parent/pp5-unlinked-deny.test.ts`, `apps/host/src/__tests__/api-admin-client-allowlist.test.ts`, `scripts/xc3-service-role-migration-batch.json`, `scripts/route-access-manifest.json`, and `scripts/admin-client-allowlist.json`. Focused run passed 5 files / 32 tests; linked evidence `artifacts/xc3-parent-report-cache-rls-live.json` shows RLS enabled with guardian select/insert/update policies and service-role policy only for service_role. |
| RCA-01 / XC-3 parent child export aggregation migration | Moved DPDP child export data aggregation in `/api/parent/children/[student_id]/export` behind the scoped authenticated `public.parent_child_export_data(uuid)` helper. The helper repeats guardian ownership using `auth.uid()` plus active/approved `guardian_student_links`, assembles the same export JSON shape, filters student notifications by `recipient_type = 'student'`, and returns table counts for audit metadata. The route still uses service-role for the existing state-event publisher, so this is recorded as a partial migration rather than an allowlist ratchet. Applied the helper to linked project `shktyoxqhundlvkiwguu`. | Added `supabase/migrations/20260710060000_xc3_parent_child_export_scoped_rpc.sql` and `apps/host/src/__tests__/parent-child-export-rpc-migration.test.ts`; updated `apps/host/src/__tests__/api/parent-child-export.test.ts` and `scripts/xc3-service-role-migration-batch.json`. Focused run passed 3 files / 18 tests; linked evidence `artifacts/xc3-parent-child-export-rpc-live.json` shows the helper is `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `public`. |
| RCA-01 / XC-3 parent erasure row-mutation migration | Moved the DPDP-sensitive guardian lookup, active-link ownership check, `data_erasure_requests` pending insert/idempotency, and grace-window cancellation update in `/api/parent/children/[student_id]/request-erasure` behind scoped authenticated RPCs. The route still uses the existing service-role event-bus publisher for `state_events`, so this is recorded as a partial migration rather than an allowlist ratchet. Applied both helpers to linked project `shktyoxqhundlvkiwguu`. | Added `supabase/migrations/20260710040000_xc3_parent_erasure_scoped_rpcs.sql` and `apps/host/src/__tests__/parent-erasure-rpc-migration.test.ts`; updated `apps/host/src/__tests__/api/parent-child-erasure.test.ts` and `scripts/xc3-service-role-migration-batch.json`. Focused run passed 4 files / 17 tests; linked evidence `artifacts/xc3-parent-erasure-rpcs-live.json` shows both helpers are `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `PUBLIC`. |
| RCA-01 / XC-3 school-admin roster read migration | Moved `/api/school-admin/students` GET roster reads off direct route-level `students` service-role queries and onto `public.school_admin_list_students(...)`, an authenticated helper that resolves the caller's active `school_admins` membership from `auth.uid()`. The route still imports service-role for POST Auth-user creation and remaining write branches, so this is recorded as a partial RLS-scoped migration rather than an allowlist ratchet. Applied the helper to linked project `shktyoxqhundlvkiwguu`. | Added `supabase/migrations/20260710050000_xc3_school_admin_students_list_rpc.sql` and `apps/host/src/__tests__/school-admin-students-rpc-migration.test.ts`; updated `scripts/xc3-service-role-migration-batch.json` and its guard. Focused run passed 2 files / 3 tests; existing school-admin students route suites plus the new guard passed 6 files / 37 tests; linked evidence `artifacts/xc3-school-admin-students-list-rpc-live.json` shows the helper is `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `public`. |
| RCA-01 / XC-3 school-admin roster active-toggle migration | Moved `/api/school-admin/students` PATCH active/inactive toggles off direct route-level `students` and `school_subscriptions` service-role queries and onto `public.school_admin_toggle_student_active(uuid, boolean)`. The helper resolves the active `school_admins` membership from `auth.uid()`, locks and verifies same-school ownership, preserves the legacy activation seat-cap response shape, and updates `students.is_active`. The route still imports service-role for POST Auth-user creation/import and the existing service-role-only seat refresh helper, so this remains a partial migration. Applied the helper to linked project `shktyoxqhundlvkiwguu`. | Added `supabase/migrations/20260710070000_xc3_school_admin_student_toggle_rpc.sql`; updated `apps/host/src/__tests__/school-admin-students-rpc-migration.test.ts`, PATCH seat-cap tests, seat-enforcement route tests, and `scripts/xc3-service-role-migration-batch.json`. Focused guard passed 4 files / 12 tests; school-admin route regression suite passed 6 files / 39 tests; linked evidence `artifacts/xc3-school-admin-student-toggle-rpc-live.json` shows the helper is `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `public`. |

## Already Covered In Repo Before This Pass

| RCA item | Current repo state |
|---|---|
| RCA-05 PAY-2 canonical unlimited price | Current repo evidence supersedes the pasted RCA's older blocker: `apps/host/src/__tests__/payments/pay2-unlimited-price-converged.test.ts`, `apps/host/src/__tests__/payments/consumer-pricing-sot-drift.test.ts`, and `apps/host/src/__tests__/cross-cutting/mobile-web-subscription-price-drift.test.ts` pin DB, web, server paisa, and mobile pricing to the converged canonical unlimited price of ₹1099/₹8799. The prior ₹1499/₹11999 divergence is documented as closed in those tests. |
| RCA-07 / RCA-15 OpenAPI/public contract | `apps/host/package.json` has `gen:openapi` and `gen:openapi:check`; public-v1 endpoint tests exist under `apps/host/src/__tests__/public-api/public-v1-endpoints.test.ts`, and public v1 spec coverage is pinned by `apps/host/src/__tests__/public-api/openapi-route.test.ts`. Coverage is still narrower than full internal route surface. |
| RCA-09 parent durable limiter | Upstash-backed limiter code is present, and the shared-secret dependency is now pinned in the Edge manifest/readiness artifact; operational activation remains pending until Supabase Edge secrets are set and smoked in the target project. |
| RCA-21 / RCA-03 class membership | Soft-delete sync and `is_active` read-path fixes are documented as landed; `scripts/tsb4-canonical-membership-cutover.json` now pins the remaining canonical-table cutover sequence, live-smoke requirement, and CEO-gated destructive retirement step. |
| RCA-04 SLC-4 / SLC-5 quiz-submit semantics | Current repo evidence supersedes the pasted RCA's older "in flight" wording for the safe slices: `apps/host/src/__tests__/lib/slc4-fallback-cap-alignment.test.ts` pins the fallback path to the canonical 7-param capped ledger writer; `apps/host/src/__tests__/quiz/slc5-anticheat-advisory-convergence.test.ts` pins client always-submit convergence; `scripts/student-learning-readiness.json` links those to canonical quiz submit route/session-id wiring and the pre-rollout `shuffle_map` gate. |
| RCA-11 grade normalization | AO-10b backfill migration, source-shape test, live verifier, and AuthContext read-time normalization are now manifest-linked and guarded. Current configured target DB verification exits 0 with `question_bank` and `curriculum_topics` conforming to P5; keep the exact target verifier output in the broad-launch evidence bundle. |
| RCA-14 super-admin PII exports | SAO-1/SAO-5 PII export tiering, REG-198, REG-202 message/context redaction, and the shared redactor barrel are now manifest-linked and guarded. Remaining action is ops communication/review, not code enablement. |

## Still Blocked Outside This Repo Pass

| RCA item | Blocker |
|---|---|
| RCA-06 SLC-1 historical XP backfill | Going-forward duplicate XP writes are now guarded as fixed by `apps/host/src/__tests__/slc1-quiz-session-trigger-dedupe.test.ts`, and `scripts/historical-xp-inflation-quantification.sql` now runs against the linked target DB. Executed export `artifacts/rca06-historical-xp-quantification.json`: 14 impacted students, `suspected_inflated_xp_total = 23565`, daily over-cap = 0, duplicate reference IDs = 0, quiz-session-vs-ledger = 0 because the live `quiz_sessions` schema no longer carries session XP. Remaining work is product-owned: CEO/product-comms decision to clamp/backfill or accept the cached-total delta. |
| RCA-04 SLC-8 server-only submit cutover / live proof | Repo now guards the SLC-4/SLC-5 safe slices, canonical route session-id wiring, and includes a provisioned `api_request_logs` telemetry source plus linked mobile legacy traffic export. The linked export currently shows no legacy mobile quiz/payment traffic. Remaining work is release-window execution after shipped mobile clients generate representative traffic on canonical `/api/quiz/submit` or `/api/v2/quiz/submit`, then re-running the verifier on that non-empty telemetry window before revocation. |
| RCA-03 / RCA-21 TSB-4 canonical membership cutover | Read-only divergence quantification and the central/teacher route-helper repoint to `class_enrollments` are now executed and guarded. Remaining work is live tenant-smoke proof against seeded tenant fixtures, then CEO-approved irreversible retirement/freeze decision for the redundant legacy table. |
| RCA-08 / RCA-09 Edge deployment state and Upstash secret values | Edge deploy/secrets manifest and shared Upstash activation requirements are now in repo and guarded. Remaining work requires Supabase project access to set/verify actual secret values, deploy/smoke `parent-portal`, and confirm logs/load behavior show durable Redis limiting rather than the in-memory fallback. |
| RCA-10 AO-3 provisioning model | Initial provisioning-path inventory is now in repo and guarded. Remaining work requires canonical institution-admin lifecycle decision/design. |
| RCA-12 full wireframe/flag/API matrix | The product surface matrix is now expanded and guarded for 13 high-demo/high-risk surfaces: student today, quiz submit, tutor, parent glance, parent data rights, teacher command center, teacher remediation/comms, school-admin roster, school-admin billing, school pulse, public API integrations, super-admin institution provisioning, and super-admin PII reports. Remaining work requires product/frontend sign-off that this matrix is the full navigation/wireframe source of truth for every surfaced CTA and hidden/dead state. |
| RCA-11 target DB grade normalization | Repo now guards AO-10b migration/read-path/verifier coverage, and current configured target proof is captured: `npx tsx scripts/verify-grade-format.ts` exited 0 with `question_bank` and `curriculum_topics` grades conforming to P5. Remaining launch evidence should retain the verifier output in the live evidence bundle for the exact target environment being promoted. |
| RCA-17 target job-health metrics | All registered host cron routes now emit awaited `job_health` rows on successful completion, and the new internal smoke route proves the writer/auth path on localhost with an injected CRON_SECRET. `npx tsx scripts/verify-job-health-live.ts --input=artifacts/rca17-job-health.json` still fails 13/13 scheduled jobs because the linked target DB has no real scheduler-produced rows yet. |
| RCA-14 lower-tier exporter notification | Repo now guards the decided PII export posture and includes an operator runbook for lower-tier notification plus post-deploy `admin_audit_log` review. Remaining work is operational execution of that runbook after deploy; do not loosen PII report types without a reviewed access decision. |
| RCA-01 / XC-3 service-role migration | Every current route is now classified and service-role/admin-client usage is manifest-justified. The first high-sensitivity migration/exception batch is explicit and guarded by `scripts/xc3-service-role-migration-batch.json`; `/api/teacher/join-class` is migrated to a scoped authenticated RPC, `/api/parent/report` is migrated to RLS-scoped cache access with linked RLS proof, `/api/parent/children/[student_id]/export`, `/api/parent/children/[student_id]/request-erasure`, and `/api/parent/children/[student_id]/erasure-status` now use scoped authenticated RPCs for DPDP data aggregation/row mutations/event publishing/status reads, `/api/parent/profile` now updates caller-owned guardian profiles through `parent_update_own_profile`, the parent invite/link-code/consent/messages routes now use auth.uid()-anchored RPCs, and `/api/school-admin/students` no longer imports the broad route-level service-role client: GET roster reads, PATCH active toggles, POST duplicate/legacy-seat/class preflight, and POST post-auth attach use scoped authenticated RPCs, while the unavoidable Supabase Auth Admin user creation is isolated behind a narrow server-only helper. The route-level service-role allowlist is ratcheted down to 249 routes and the XC-3 batch manifest now asserts that count against the live allowlist. Remaining repo-owned service-role migration work in this batch is reviewed exception posture for cron/webhooks/super-admin exports and further narrowing of service-role-only shared helpers such as seat refresh where feasible. |
| RCA-19 live tenant isolation smoke | Repo now includes `scripts/verify-live-tenant-isolation-smoke.ts` for parent, teacher, and school-admin cross-tenant probes. Remaining work requires a staging/live target with seeded tenant A/B role tokens and resource IDs, then running the script with `LIVE_TENANT_SMOKE_BASE_URL`, `LIVE_TENANT_SMOKE_PARENT_A_TOKEN`, `LIVE_TENANT_SMOKE_TEACHER_A_TOKEN`, `LIVE_TENANT_SMOKE_SCHOOL_ADMIN_A_TOKEN`, `LIVE_TENANT_SMOKE_STUDENT_B_ID`, `LIVE_TENANT_SMOKE_CLASS_B_ID`, and `LIVE_TENANT_SMOKE_SCHOOL_B_ID`. |
| RCA-17 / RCA-23 Supabase Edge job health and full incident rollout | Vercel cron registry, awaited host cron success instrumentation, localhost smoke writer proof, live job-health verifier, initial host-route/Edge incident-ID spine, and live incident-ID verifier are now in repo and guarded. RCA-17 target export still fails because no real scheduled-job `job_health` metrics exist yet. Remaining work requires deploying/enabling the cron success instrumentation in the target scheduler path with `CRON_SECRET`, allowing/triggering real cron successes, re-running `npx tsx scripts/verify-job-health-live.ts --input=<rows.json>`, sampling a target route and validating its `X-Request-Id` with `npx tsx scripts/verify-incident-id-live.ts --print-sql=<request-id>` plus `--input=<evidence.json>`, and expanding incident ID propagation beyond the pinned `board-score` chain across all priority routes/functions/RPCs/PostHog events. |
| RCA-22 legacy quiz/API revocation | Inventory and live traffic verifier are now in repo and guarded. Dropping or revoking legacy quiz RPC/API paths requires confirmed mobile/web cutover to server-side submit, SLC-5 mastery semantics, and a clean telemetry-backed release window from `scripts/verify-mobile-legacy-traffic-live.ts`. |
| RCA-25 mobile rollout/repoint | The generated `/v2` Dart client is pinned to the OpenAPI/host route surface, `api_request_logs` now exists on the linked DB, and the linked mobile legacy verifier passes with zero legacy rows. Remaining work is to rerun after representative mobile release traffic exists, then resolve any legacy quiz/payment findings before revoking compatibility paths. |
| RCA-20 live certification execution | Repo now guards the certification E2E suite, account seeding, teardown, traffic traceability, rollback, and payment safety gates. Remaining work requires an operator-approved target, a recorded `CERTIFICATION_RUN_ID`, resolved/accepted CERT-17 payment credential posture, and product decisions for CERT-FE-01 and CERT-07 before treating browser certification as broad-launch proof. |

## Latest Repo-Owned Release Gate

Executed/rerun on 2026-07-10 IST:

1. `npm run type-check --workspaces --if-present` — passed.
2. `npm run lint --workspaces --if-present` — passed.
3. Host RCA readiness Vitest batch — 34 files / 96 tests passed.
4. `npm run eval:tenant-isolation -w apps/host` — passed, 362 routes scanned with no baseline regression.
5. `npx tsx scripts/pre-rollout-checklist.ts` — passed, 14/14 checks.
6. `npm run build -w apps/host` — passed.
7. `npm run gen:openapi:check -w apps/host` — passed.
8. Focused RCA telemetry/job-health suite — `npx vitest run src/__tests__/api/cron-job-health-instrumentation.test.ts src/__tests__/mobile-request-log-instrumentation.test.ts src/__tests__/mobile-legacy-traffic-verifier.test.ts src/__tests__/job-health-live-verifier.test.ts` passed, 4 files / 11 tests.
9. Focused TSB canonical membership suite — `npx vitest run src/__tests__/tsb4-enrollments-rls-reconcile.test.ts src/__tests__/tsb4-canonical-membership-cutover-readiness.test.ts src/__tests__/tsb4-divergence-quantification.test.ts src/__tests__/security/tenant-isolation-role-scoped-apis.test.ts src/__tests__/api/teacher/active-enrollment-scoping.test.ts src/__tests__/api/teacher/remediation/route.test.ts src/__tests__/api/teacher/parent-notify/route.test.ts` passed, 7 files / 80 tests.
10. Focused XC-3 teacher join-class migration suite — `npx vitest run src/__tests__/api/track-b/teacher-join-class.test.ts src/__tests__/teacher-join-class-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 4 files / 14 tests.
11. Post-XC-3 rerun — `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 362 routes; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0.
12. Focused XC-3 parent report cache migration suite — `npx vitest run src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts src/__tests__/api/parent/pp5-unlinked-deny.test.ts src/__tests__/parent-report-cache-rls-corrective.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts` passed, 5 files / 32 tests.
13. Post-parent-report rerun — `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 362 routes; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0.
14. Focused XC-3 parent erasure scoped-RPC suite — `npx vitest run src/__tests__/api/parent-child-erasure.test.ts src/__tests__/parent-erasure-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 4 files / 17 tests.
15. Post-parent-erasure rerun — `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `npx vitest run src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/parent-child-erasure.test.ts src/__tests__/parent-erasure-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 5 files / 23 tests; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 362 routes; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0.
16. Focused XC-3 school-admin roster read migration suite — `npx vitest run src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts` passed, 2 files / 3 tests.
17. School-admin roster regression rerun — `npx vitest run src/__tests__/api/school-admin-students-post.test.ts src/__tests__/api/school-admin-students-seat-cap.test.ts src/__tests__/api/school-admin/seat-enforcement-routes.test.ts src/__tests__/api/school-admin/seat-enforcement-flag-off.test.ts src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts` passed, 6 files / 37 tests; `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-school-admin-rpc.log`.
18. Focused XC-3 parent child export migration suite — `npx vitest run src/__tests__/api/parent-child-export.test.ts src/__tests__/parent-child-export-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts` passed, 3 files / 18 tests.
19. Parent export scoped-RPC rerun — `npx vitest run src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts src/__tests__/api/parent-child-export.test.ts src/__tests__/parent-child-export-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts` passed, 5 files / 25 tests; `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 362 routes; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-parent-export-rpc.log`.
20. Focused XC-3 school-admin active-toggle migration suite — `npx vitest run src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 4 files / 12 tests.
21. School-admin PATCH scoped-RPC rerun — `npx vitest run src/__tests__/api/school-admin-students-post.test.ts src/__tests__/api/school-admin-students-seat-cap.test.ts src/__tests__/api/school-admin/seat-enforcement-flag-off.test.ts src/__tests__/api/school-admin/seat-enforcement-routes.test.ts src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts` passed, 6 files / 39 tests; `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 362 routes; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-school-admin-toggle-rpc.log`.
22. Focused XC-3 parent child state-event publisher migration suite — red run first failed on the old `publishEvent(supabaseAdmin)` route shape and missing migration; after implementation, `npx vitest run src/__tests__/parent-child-export-rpc-migration.test.ts src/__tests__/parent-erasure-rpc-migration.test.ts src/__tests__/parent-child-state-event-rpc-migration.test.ts src/__tests__/api/parent-child-export.test.ts src/__tests__/api/parent-child-erasure.test.ts` passed, 5 files / 34 tests. Static ratchet guards then passed with `npx vitest run src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts src/__tests__/parent-child-export-rpc-migration.test.ts src/__tests__/parent-erasure-rpc-migration.test.ts src/__tests__/parent-child-state-event-rpc-migration.test.ts`, 6 files / 13 tests. Full focused regression rerun passed 8 files / 42 tests; `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 362 routes; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-parent-event-publisher-rpc-rerun.log`. `supabase db query --linked -f supabase/migrations/20260710080000_xc3_parent_child_state_event_rpc.sql` exited 0; linked catalog proof is captured at `artifacts/xc3-parent-child-state-event-rpc-live.json`.
23. Focused XC-3 school-admin POST attach migration suite — red run first failed on the old direct `students.update`/missing migration/no attach RPC path; after implementation, `npx vitest run src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/api/school-admin-students-post.test.ts` passed, 2 files / 15 tests. The broader school-admin POST/PATCH/seat regression cluster then passed with `npx vitest run src/__tests__/api/school-admin-students-post.test.ts src/__tests__/api/school-admin-students-seat-cap.test.ts src/__tests__/api/school-admin/seat-enforcement-flag-off.test.ts src/__tests__/api/school-admin/seat-enforcement-routes.test.ts src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts`, 6 files / 41 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; RCA static guards passed 3 files / 8 tests; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 362 routes; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-school-admin-attach-rpc.log`. `supabase db query --linked -f supabase/migrations/20260710090000_xc3_school_admin_student_attach_rpc.sql` exited 0; linked catalog proof is captured at `artifacts/xc3-school-admin-student-attach-rpc-live.json`.
24. Focused XC-3 school-admin POST preflight migration suite — red run first failed on the old direct preflight route shape and missing migration; after implementation, `npx vitest run src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/api/school-admin-students-post.test.ts` passed, 2 files / 18 tests. The broader school-admin POST/PATCH/seat regression cluster then passed with `npx vitest run src/__tests__/api/school-admin-students-post.test.ts src/__tests__/api/school-admin-students-seat-cap.test.ts src/__tests__/api/school-admin/seat-enforcement-flag-off.test.ts src/__tests__/api/school-admin/seat-enforcement-routes.test.ts src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts`, 6 files / 44 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; RCA static guards passed 3 files / 8 tests; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 362 routes; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-school-admin-preflight-rpc-rerun.log` after an earlier 5-minute timeout on the first captured build attempt. `supabase db query --linked -f supabase/migrations/20260710100000_xc3_school_admin_student_create_preflight_rpc.sql` exited 0; linked catalog proof is captured at `artifacts/xc3-school-admin-student-create-preflight-rpc-live.json`.
25. Focused RCA-13 Hindi output-screen coverage — red run first failed because Hindi/Devanagari/Hinglish abusive-token examples passed the `screenStudentFacingText` blocklist and the RCA-13 manifest still listed Hindi-token coverage as open; after implementation, `npx vitest run src/__tests__/lib/ai/validation/output-screen.test.ts src/__tests__/lib/ai/validation/output-screen-deno-parity.test.ts src/__tests__/foxy-rag-readiness.test.ts` passed, 3 files / 42 tests. The native Deno twin check `deno test __tests__/output-screen.test.ts` passed, 3/3 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only. Fresh captured `npm run build -w apps/host` first exposed an opaque Next/webpack exit with Unicode property-boundary regexes; replacing `\p{L}`/`\p{N}` with explicit ASCII+Devanagari ranges kept the tests green and the rerun exited 0 via `artifacts/host-build-foxy-hindi-output-screen-fixed.log`.
26. Focused RCA-13 FOX-7 legacy fallback output backstop — red run first failed because unsafe legacy intent-router output was returned on the wire; after implementation, `npx vitest run src/__tests__/api/foxy/output-safety-backstop.test.ts src/__tests__/foxy-rag-readiness.test.ts` passed, 2 files / 5 tests. Expanded RCA-13 cluster `npx vitest run src/__tests__/api/foxy/output-safety-backstop.test.ts src/__tests__/lib/ai/validation/output-screen.test.ts src/__tests__/lib/ai/validation/output-screen-deno-parity.test.ts src/__tests__/foxy-rag-readiness.test.ts` passed, 4 files / 46 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-foxy-legacy-output-backstop.log`.
27. Focused RCA-13 streaming first-paint output backstop — red run first failed because the drained SSE body contained the unsafe upstream `text` frame before the synthetic abstain (`expected ... not to contain 'fucking'`). After implementation, targeted `npx vitest run src/__tests__/api/foxy/streaming-structured-persistence.test.ts -t "does not send unsafe text deltas"` passed, then the full streaming persistence file passed 1 file / 4 tests. Expanded RCA-13 cluster `npx vitest run src/__tests__/api/foxy/streaming-structured-persistence.test.ts src/__tests__/api/foxy/output-safety-backstop.test.ts src/__tests__/lib/ai/validation/output-screen.test.ts src/__tests__/lib/ai/validation/output-screen-deno-parity.test.ts src/__tests__/foxy-rag-readiness.test.ts src/__tests__/foxy-streaming.test.ts src/__tests__/foxy-streaming-json-fallback-abstain.test.ts` passed, 7 files / 72 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 362 routes; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-foxy-streaming-first-paint-backstop.log`.
28. Focused RCA-17 localhost cron job-health smoke — red run first failed because `src/__tests__/api/cron/job-health-smoke.test.ts` could not import the missing route. After implementation, `npx vitest run src/__tests__/api/cron/job-health-smoke.test.ts` passed, 1 file / 4 tests. Route-access guard failed until `scripts/route-access-manifest.json` was regenerated to 364 routes, and release-gate coverage failed until `scripts/product-readiness-release-gate.ts` included the new smoke test. The focused RCA-17/manifest cluster `npx vitest run src/__tests__/api/cron/job-health-smoke.test.ts src/__tests__/api/cron-job-health-instrumentation.test.ts src/__tests__/job-health-live-verifier.test.ts src/__tests__/api/cron-job-registry.test.ts src/__tests__/api/route-access-manifest.test.ts src/__tests__/product-readiness-release-gate.test.ts src/__tests__/eval/tenant-isolation-evaluator.test.ts` passed, 7 files / 28 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-cron-job-health-smoke.log`.
29. Focused XC-3 school-admin class preflight migration — red run first failed on the old route-level `.from('classes')` service-role precheck, missing `p_class_id` in `school_admin_student_create_preflight`, and missing follow-up migration. After implementation, `npx vitest run src/__tests__/school-admin-students-rpc-migration.test.ts` passed, 1 file / 10 tests, and `npx vitest run src/__tests__/api/school-admin-students-post.test.ts` passed, 1 file / 10 tests. The broader school-admin/XC-3 cluster `npx vitest run src/__tests__/api/school-admin-students-post.test.ts src/__tests__/api/school-admin-students-seat-cap.test.ts src/__tests__/api/school-admin/seat-enforcement-flag-off.test.ts src/__tests__/api/school-admin/seat-enforcement-routes.test.ts src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 7 files / 47 tests. `supabase db query --linked -f supabase/migrations/20260710110000_xc3_school_admin_student_create_class_preflight_rpc.sql` exited 0; `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-school-admin-class-preflight-rpc-final.log`.
30. Focused XC-3 school-admin Auth Admin narrowing — red run first failed because `/api/school-admin/students` still imported `@alfanumrik/lib/supabase-admin`, and the admin-client allowlist was still pinned at 265. After implementation, `npx vitest run src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 4 files / 19 tests. The broader school-admin/XC-3 cluster `npx vitest run src/__tests__/api/school-admin-students-post.test.ts src/__tests__/api/school-admin-students-seat-cap.test.ts src/__tests__/api/school-admin/seat-enforcement-flag-off.test.ts src/__tests__/api/school-admin/seat-enforcement-routes.test.ts src/__tests__/school-admin-students-rpc-migration.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 8 files / 54 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-school-admin-auth-admin-helper.log`.
31. Focused RCA-12 product surface matrix expansion — red run first failed because the matrix still covered only seven surfaces (`expected 7 to be greater than or equal to 13`). After implementation, `npx vitest run src/__tests__/product-surface-matrix.test.ts src/__tests__/entitlements/institution-entitlements-rls.test.ts src/__tests__/entitlements/resolver.test.ts` passed, 3 files / 47 tests, with 13 guarded surfaces and the missing `ff_institution_entitlements_v1` default-registration drift closed. After aligning the operator gate wording to product sign-off on the expanded 13-surface matrix, `npx vitest run src/__tests__/product-readiness-release-gate.test.ts src/__tests__/live-readiness-evidence.test.ts src/__tests__/product-surface-matrix.test.ts` passed, 3 files / 11 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed; fresh captured `npm run build -w apps/host` exited 0 via `artifacts/host-build-product-surface-matrix-expansion.log`.
32. Focused RCA-01 / XC-3 parent erasure-status migration — red run first failed because `/api/parent/children/[student_id]/erasure-status` still used route-level `supabaseAdmin` table reads and the follow-up `parent_child_erasure_status` migration did not exist. After implementation, the route calls the scoped authenticated RPC, `scripts/admin-client-allowlist.json` ratcheted 264 → 263, route-access metadata was regenerated, and `supabase db query --linked -f supabase/migrations/20260710120000_xc3_parent_erasure_status_rpc.sql` exited 0. Linked catalog proof is captured in `artifacts/xc3-parent-erasure-status-rpc-live.json` with `security_definer = true`, `search_path=public`, `authenticated_execute = true`, and `anon/public_execute = false`. Focused parent/XC-3 cluster `npx vitest run --reporter=dot src/__tests__/api/parent-child-erasure.test.ts src/__tests__/parent-erasure-rpc-migration.test.ts src/__tests__/parent-child-state-event-rpc-migration.test.ts src/__tests__/api/parent/pp5-unlinked-deny.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/xc3-service-role-migration-batch.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 7 files / 50 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed. The first captured build reached successful compile/typecheck/static generation then hit an opaque Next worker exit `4294967295`; the captured rerun exited 0 via `artifacts/host-build-parent-erasure-status-rpc-rerun.log`.
33. Focused RCA-01 / XC-3 parent profile update migration — red run first failed because `/api/parent/profile` still used route-level `supabaseAdmin` writes and `supabase/migrations/20260710130000_xc3_parent_profile_update_rpc.sql` did not exist. After implementation, the route calls scoped authenticated `parent_update_own_profile`, validates name/phone in the route, forwards only update-mask fields to the RPC, and no longer forwards body-supplied ids. `scripts/admin-client-allowlist.json` ratcheted 263 → 262 and route-access metadata was regenerated. `supabase db query --linked -f supabase/migrations/20260710130000_xc3_parent_profile_update_rpc.sql` exited 0; linked catalog proof is captured in `artifacts/xc3-parent-profile-update-rpc-live.json` with `security_definer = true`, `search_path=public`, `authenticated_execute = true`, and `anon/public_execute = false`. Focused tests `npx vitest run src/__tests__/api/parent/profile-auth-gate.test.ts src/__tests__/parent-profile-rpc-migration.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 4 files / 15 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed. Build initially hit a live dev lock and then stale `.next/dev/types/validator.ts`; after stopping the dev chain and clearing `.next`, clean captured build exited 0 via `artifacts/host-build-parent-profile-rpc-clean.log`.
34. Focused RCA-01 / XC-3 parent notifications migration — red run first failed because `/api/parent/notifications`, `/api/parent/notifications/[id]/read`, and `/api/parent/notifications/mark-all-read` still used route-level `supabaseAdmin` table access and `supabase/migrations/20260710140000_xc3_parent_notifications_rpcs.sql` did not exist. After implementation, the routes call scoped authenticated `parent_list_notifications`, `parent_mark_notification_read`, and `parent_mark_all_notifications_read` RPCs; `scripts/admin-client-allowlist.json` ratcheted 262 → 259 and route-access metadata was regenerated. `supabase db query --linked -f supabase/migrations/20260710140000_xc3_parent_notifications_rpcs.sql` exited 0; linked catalog proof is captured in `artifacts/xc3-parent-notifications-rpcs-live.json` with all three helpers `security_definer = true`, `search_path=public`, `authenticated_execute = true`, and `anon/public_execute = false`. Red/green focused tests `npx vitest run src/__tests__/api/parent-notifications.test.ts src/__tests__/parent-notifications-rpc-migration.test.ts src/__tests__/api/pagination.test.ts` passed after implementation, 3 files / 23 tests; wider focused static cluster `npx vitest run src/__tests__/api/parent-notifications.test.ts src/__tests__/parent-notifications-rpc-migration.test.ts src/__tests__/api/pagination.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 5 files / 30 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed. Workspace build attempts first exposed the repo's intermittent Next worker `4294967295` and then a stale overlapping-build `proxy.js.nft.json` trace error; after stopping build/dev processes and clearing `.next`, the single direct captured build completed via `artifacts/host-build-parent-notifications-rpcs-final.log` with successful compile, typecheck, static generation, and final route summary.
35. Focused RCA-01 / XC-3 parent calendar RLS-scoped migration — red run first failed because `/api/parent/calendar` still imported `@alfanumrik/lib/supabase-admin` for child calendar aggregation reads. After implementation, the route keeps the existing `authorizeRequest('child.view_progress')` and `canAccessStudent(auth.userId, student_id)` deny boundary, then runs `class_enrollments`, `assignments`, `school_exams`, and `quiz_sessions` reads through an authenticated `createServerClient` request client carrying the caller Authorization header/cookies. `scripts/admin-client-allowlist.json` ratcheted 259 → 258 and route-access metadata was regenerated. Focused cluster `npx vitest run src/__tests__/api/parent/parent-calendar.test.ts src/__tests__/api/parent/pp5-unlinked-deny.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 4 files / 38 tests. `npm run type-check --workspaces --if-present` passed after clearing stale orphaned timed-out `tsc` children; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed. Captured host build with `auth-guard` plus direct `next build --webpack --debug` exited 0 via `artifacts/host-build-parent-calendar-rls.log`.
36. Focused RCA-01 / XC-3 parent billing RLS-scoped migration — red run first failed because `/api/parent/billing` still imported `@alfanumrik/lib/supabase-admin` for billing aggregation reads. After implementation, the route keeps the guardian/profile and `listChildrenForGuardian(auth.userId)` ownership boundary, then runs `student_subscriptions`, `subscription_plans`, and `payment_history` reads through an authenticated `createServerClient` request client carrying the caller Authorization header/cookies. `scripts/admin-client-allowlist.json` ratcheted 258 → 257 and route-access metadata was regenerated; the remaining parent route-level service-role allowlist is now the eight invite/link-code/consent/messages routes. Focused cluster `npx vitest run src/__tests__/api/parent-billing.test.ts src/__tests__/api/parent/pp5-unlinked-deny.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts` passed, 4 files / 44 tests. `npm run type-check --workspaces --if-present` passed after clearing stale `.next/dev` generated types; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed. Host build attempts exposed a Next 16 proxy build mismatch from the legacy `export { proxy as middleware }` alias in `src/proxy.ts`; a new auth structural guard now rejects that alias, proxy/auth tests passed 5 files / 216 tests, and `.next/server/proxy.js.nft.json` is generated. Local full build finalization is not yet proven in this Windows shell: Node 22 plus local-only `NEXT_DISABLE_WEBPACK_BUILD_WORKER=1 NEXT_WEBPACK_MEMORY_OPTIMIZATIONS=1` reached compile/TypeScript/minify with existing Sentry/Prisma warnings and proxy trace output, but exited before `BUILD_ID`, `routes-manifest.json`, and standalone output were written. Localhost health remained green after the slice.
37. Focused RCA-01 / XC-3 parent approve-link RPC migration — red run first failed because `/api/parent/approve-link` still imported `@alfanumrik/lib/supabase-admin` and `@alfanumrik/lib/domains/relationship` for student lookup, pending-link ownership check, and `guardian_student_links` status update. After implementation, the route keeps cookie-session authentication and body validation, then calls the authenticated `public.student_review_guardian_link(uuid, text)` helper. The helper resolves the student from `auth.uid()`, updates only that student's pending link, records audit metadata, and grants execute to `authenticated` while denying `anon`. `scripts/admin-client-allowlist.json` ratcheted 257 -> 256 and route-access metadata was regenerated; the remaining parent route-level service-role allowlist is now seven invite/link-code/consent/messages routes. The first linked `supabase db push --linked --yes` exposed a syntax defect in pending migration `20260707010000_rca_final_fixes.sql` (`match_rag_chunks_ncert` quality-gate predicates missing closing parentheses); after patching that migration, `supabase db push --linked --yes` applied all pending migrations through `20260710150000_xc3_student_review_guardian_link_rpc.sql`. Remote proof: `supabase migration list` shows local/remote `20260710150000`, `supabase db push --linked --dry-run` reports "Remote database is up to date", and linked catalog query shows `student_review_guardian_link` uses `auth.uid()`, `authenticated_can_execute = true`, and `anon_can_execute = false`. Focused cluster `npx vitest run src/__tests__/api/parent/approve-link/route.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts src/__tests__/schema/rpc-conformance.test.ts` passed, 4 files / 21 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed. Localhost health remained green after restarting the host dev server on `127.0.0.1:3000`.
38. Focused RCA-01 / XC-3 parent accept-invite RPC migration — red run first failed because `/api/parent/accept-invite` still imported `@alfanumrik/lib/supabase-admin` for guardian profile lookup, legacy invite RPC invocation, student-name lookup, and NULL-guardian placeholder cleanup. After implementation, the route keeps cookie-session authentication, strict `link_code` validation, and P13 truncated-code logging, then calls the authenticated `public.parent_accept_invite_code(text)` helper. The helper resolves the guardian from `auth.uid()`, delegates to the existing idempotent invite-link helper using `auth.uid()`, resolves the student name, and retires the pending placeholder row inside the database. `scripts/admin-client-allowlist.json` ratcheted 256 -> 255 and route-access metadata was regenerated; the remaining parent route-level service-role allowlist is now six consent/link-code/messages routes. `supabase db push --linked --dry-run` first showed only `20260710160000_xc3_parent_accept_invite_rpc.sql` pending; `supabase db push --linked --yes` applied it. Remote proof: `supabase migration list` shows local/remote `20260710160000`, `supabase db push --linked --dry-run` reports "Remote database is up to date", and linked catalog query shows `parent_accept_invite_code` uses `auth.uid()`, `authenticated_can_execute = true`, and `anon_can_execute = false`. Focused cluster `npx vitest run src/__tests__/api/track-b/accept-invite.test.ts src/__tests__/api/parent/pp2-filter-injection-routes.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts src/__tests__/schema/rpc-conformance.test.ts` passed, 5 files / 23 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed. Localhost health remained green after restarting the host dev server on `127.0.0.1:3000`.
39. Focused RCA-01 / XC-3 parent link-code OTP RPC migration — red run first failed because `/api/parent/link-code/request-otp` and `/api/parent/link-code/redeem` still imported `@alfanumrik/lib/supabase-admin` for student lookup, challenge insert/update/delete, audit writes, guardian lookup, and invite redemption. After implementation, request-otp keeps cookie-session authentication, rate limiting, strict `link_code` validation, OTP generation, and email delivery in the route, then calls authenticated `public.parent_request_link_code_otp(...)` for code resolution, cooldown, challenge insertion, and audit. Redeem now calls authenticated `public.parent_redeem_link_code_otp(...)`, keeping OTP hash verification, lockout, guardian lookup, domain RPC delegation, challenge deletion, and audit inside the database so challenge hashes never leave the scoped boundary. `scripts/admin-client-allowlist.json` ratcheted 255 -> 253 and route-access metadata was regenerated; the remaining parent route-level service-role allowlist is now consent plus parent messages routes. `supabase db push --linked --dry-run` showed only `20260710170000_xc3_parent_link_code_otp_rpcs.sql` pending; `supabase db push --linked --yes` applied it. Remote proof: `supabase migration list` shows local/remote `20260710170000`, `supabase db push --linked --dry-run` reports "Remote database is up to date", and linked catalog query shows both OTP RPCs use `auth.uid()`, grant execute to `authenticated`, and deny `anon`; `parent_redeem_link_code_otp` also uses `extensions.digest` for DB-side OTP hash comparison. Focused cluster `npx vitest run src/__tests__/api/link-code-otp.test.ts src/__tests__/api/parent/pp2-filter-injection-routes.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts src/__tests__/schema/rpc-conformance.test.ts` passed, 5 files / 28 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed. Localhost probes against `127.0.0.1:3000` returned 200 for `/api/health` with `{"status":"ok","db":"ok"}`, 200 for `/api/public/v1/openapi` with length 15448, and 200 for `/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
40. Focused RCA-01 / XC-3 parent consent RPC migration — red run first failed because `/api/parent/consent` still imported `@alfanumrik/lib/supabase-admin` for guardian lookup, active-link checks, consent row mutation/listing, and state-event publishing, while `supabase/migrations/20260710180000_xc3_parent_consent_rpcs.sql` did not exist. After implementation, the route keeps Supabase session validation, Zod body validation, scope allowlisting, and response mapping, then calls authenticated `public.parent_record_consent(...)`, `public.parent_revoke_consent(...)`, and `public.parent_list_active_consents()`. The helpers resolve the guardian from `auth.uid()`, enforce active/approved `guardian_student_links`, insert or soft-revoke `parental_consent`, emit `state_events`, and write `audit_logs` rows inside the database boundary. `scripts/admin-client-allowlist.json` ratcheted 253 -> 252 and route-access metadata was regenerated; the remaining parent route-level service-role allowlist is now the three parent messages routes. `supabase db push --linked --dry-run` showed only `20260710180000_xc3_parent_consent_rpcs.sql` pending; `supabase db push --linked --yes` applied it. Remote proof: `supabase migration list` shows local/remote `20260710180000`, `supabase db push --linked --dry-run` reports "Remote database is up to date", and linked catalog query shows all three consent RPCs use `auth.uid()`, grant execute to `authenticated`, and deny `anon`; the grant/revoke helpers also write `state_events` and `audit_logs`. Focused cluster `npx vitest run src/__tests__/api/parent-consent.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts src/__tests__/schema/rpc-conformance.test.ts` passed, 4 files / 27 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed. Localhost probes against `127.0.0.1:3000` returned 200 for `/api/health` with `{"status":"ok","db":"ok"}`, 200 for `/api/public/v1/openapi` with length 15448, and 200 for `/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
41. Focused RCA-01 / XC-3 parent messages RPC migration — red/green focused coverage moved the three remaining parent message routes off route-level `@alfanumrik/lib/supabase-admin`: `/api/parent/messages`, `/api/parent/messages/threads`, and `/api/parent/messages/threads/[id]/messages`. After implementation, the routes keep RBAC and request validation, then call authenticated `public.parent_send_teacher_message(...)`, `public.parent_list_message_threads(integer)`, and `public.parent_list_thread_messages(uuid, timestamptz, integer)`. The helpers resolve the guardian from `auth.uid()`, enforce thread/guardian ownership, validate guardian-student links, insert guardian messages, emit `state_events`, insert teacher notifications, list enriched threads, and mark teacher-sent rows read inside the database boundary. `scripts/admin-client-allowlist.json` ratcheted 252 -> 249 and route-access metadata was regenerated; this drains the parent invite/link-code/consent/messages route-level service-role batch. `supabase db push --linked --dry-run` showed only `20260710190000_xc3_parent_messages_rpcs.sql` pending; `supabase db push --linked --yes` applied it. Remote proof: `supabase migration list` shows local/remote `20260710190000`, `supabase db push --linked --dry-run` reports "Remote database is up to date", and linked catalog query shows all three parent messaging RPCs use `auth.uid()`, grant execute to `authenticated`, and deny `anon`; the send helper also writes `state_events` and `notifications`. Focused cluster `npx vitest run src/__tests__/api/teacher-parent-messaging.test.ts src/__tests__/parent-messages-rpc-migration.test.ts src/__tests__/api-admin-client-allowlist.test.ts src/__tests__/api/route-access-manifest.test.ts src/__tests__/schema/rpc-conformance.test.ts` passed, 5 files / 30 tests. `npm run type-check --workspaces --if-present` passed; `npm run lint --workspaces --if-present` passed; `git diff --check` exited 0 with CRLF warnings only; `npm run eval:tenant-isolation -w apps/host` passed with 363 current routes and no baseline regression; `npx tsx scripts/pre-rollout-checklist.ts` passed 14/14; `npm run gen:openapi:check -w apps/host` passed. Localhost probes against `127.0.0.1:3000` returned 200 for `/api/health` with `{"status":"ok","db":"ok"}`, 200 for `/api/public/v1/openapi` with length 15448, and 200 for `/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
42. RCA-01 / RCA-20 XC-3 release-evidence synchronization — tightened `apps/host/src/__tests__/xc3-service-role-migration-batch.test.ts` so `scripts/xc3-service-role-migration-batch.json.totalAdminClientRoutesPinned` must match the live `scripts/admin-client-allowlist.json` count, then updated the manifest from 263 to 249 and added the parent invite/link-code/consent/messages drain as a migrated scoped-RPC entry. Updated the RCA-01 operator gate in `scripts/product-readiness-release-gate.ts` and `scripts/live-readiness-evidence-manifest.json` from stale "first-batch RLS migration execution" wording to the actual remaining service-role exception review / next narrowing pass. Red run first failed with `expected 263 to be 249`; after the manifest update, `npx vitest run src/__tests__/xc3-service-role-migration-batch.test.ts src/__tests__/api-admin-client-allowlist.test.ts` passed, 2 files / 7 tests. Red run for release-gate evidence first failed because the live evidence manifest still had `xc3-migration-execution`; after updating the runner and manifest, `npx vitest run src/__tests__/product-readiness-release-gate.test.ts src/__tests__/live-readiness-evidence.test.ts` passed, 2 files / 16 tests. `npx tsx scripts/product-readiness-release-gate.ts --list` now shows RCA-01 operator step 13 as `XC-3 service-role exception review and next narrowing pass`, not first-batch execution.
43. RCA-11 target DB grade-format verification — ran the target-backed verifier with the configured `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`: `npx tsx scripts/verify-grade-format.ts` exited 0. Output showed `question_bank` distinct grades `[ '11', '12' ]` with no invalid P5 values, `curriculum_topics` grades `[ '10', '11', '12', '6', '7', '8', '9' ]` all conforming to P5, and `rag_content_chunks` informationally using `Grade 6` through `Grade 12` by design. This closes the repo/target proof previously listed for RCA-11 target DB grade normalization in the configured environment.
44. RCA-20 full repo-owned product-readiness release gate rerun — ran the monolithic release gate with an extended timeout: `npx tsx scripts/product-readiness-release-gate.ts` exited 0 on 2026-07-10. The run passed type-check, lint, DevOps policy contract, full host production build, every focused RCA Vitest gate, static tenant isolation, grounded-answer pre-rollout checklist, and OpenAPI drift check. Final summary: `39/39 repo gates passed`. The host build completed through compile, TypeScript, 381 static pages, route summary, and build traces; remaining output was the known Prisma/OpenTelemetry/Sentry warning, not a failing gate.

The monolithic `npx tsx scripts/product-readiness-release-gate.ts` runner now completes locally with `39/39 repo gates passed`. A previous 15-minute timeout is superseded by the 2026-07-10 extended-timeout run above.

## Latest Localhost Backend Health

Executed against the running local Next server on 2026-07-10 IST:

1. `GET http://localhost:3000/api/health` — 200, `{"status":"ok","db":"ok"}` after the latest build.
2. `GET http://localhost:3000/api/public/v1/openapi` — 200 after route path fix and latest build.
3. `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` — 200, `{"enabled":true}` after the latest build.
4. `npx tsx scripts/verify-grade-format.ts` — exit 0; `question_bank` and `curriculum_topics` grades conform to P5.
5. `npx tsx scripts/verify-feature-flag-matrix.ts --env=production` — exit 0; `checked: 120`, no missing/mismatched/unexpected flags.
6. `npx tsx scripts/verify-feature-flag-matrix.ts --env=staging` — exit 0; `checked: 120`, no missing/mismatched/unexpected flags.
7. `npx tsx scripts/verify-mobile-legacy-traffic-live.ts --input=artifacts/rca04-mobile-legacy-traffic.json` — exit 0; linked legacy export returned 0 matching rows and no legacy mobile quiz/payment traffic. Aggregate table sanity check `artifacts/rca25-api-request-log-count.sql` returned `api_request_logs_count = 35`, confirming the linked telemetry table exists and is receiving rows.
8. `npx tsx scripts/verify-job-health-live.ts --input=artifacts/rca17-job-health.json` — exit 1; 0/13 live scheduler success metrics present yet. Code instrumentation is landed, but real cron execution remains required.
9. `supabase db query --linked -f scripts/tsb4-class-membership-divergence-quantification.sql -o json > artifacts/tsb4-class-membership-divergence.json` — exit 0; only `matched_or_both_inactive` rows, `pair_count = 18`, no direction-A leak, direction-B authorization-widening, canonical-only, or legacy-only rows.
10. Post-parent-event-publisher localhost probes — `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}` on retry after an initial 20-second timeout; `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448; `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
11. Post-school-admin-attach localhost probes — `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`; `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448; `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
12. Post-school-admin-preflight localhost probes — `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`; `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448; `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
13. Post-Foxy-Hindi-output-screen localhost probes — `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`; `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448; `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
14. Post-Foxy-legacy-output-backstop localhost probes — `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`; `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448; `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
15. Post-Foxy-streaming-first-paint-backstop localhost probes — `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`; `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448; `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
16. RCA-17 cron job-health smoke localhost proof — started the rebuilt host on port 3017 with injected `CRON_SECRET=local-cron-smoke-secret`. `POST http://localhost:3017/api/internal/cron/job-health-smoke` returned 401 without a secret, and the same POST with `x-cron-secret` returned 200 with `{"ok":true,"path":"/api/internal/cron/job-health-smoke","metric":"ops.cron.job_health_smoke.last_success_at"}`. The temporary port-3017 server was stopped after the smoke. Post-smoke port-3000 probes also returned 200 for `/api/health`, `/api/public/v1/openapi`, and `/api/feature-flags/check?flag=ff_today_home_v1`.
17. Post-school-admin-class-preflight localhost probes — after the linked helper migration and fresh host build, `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`, `GET http://localhost:3000/api/public/v1/openapi` returned 200, and `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
18. Post-school-admin-auth-admin-helper localhost probes — after removing the `/api/school-admin/students` route-level `supabase-admin` import and rebuilding, `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`, `GET http://localhost:3000/api/public/v1/openapi` returned 200, and `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
19. Post-product-surface-matrix-expansion localhost probes — after the expanded RCA-12 matrix guard and fresh host build, `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`, `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448, and `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
20. Post-parent-erasure-status-rpc localhost probes — after applying the linked `parent_child_erasure_status` helper and rerunning the host build, `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`, `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448, and `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
21. Post-parent-profile-rpc localhost probes — after applying the linked `parent_update_own_profile` helper, clearing stale `.next` generated types, rebuilding, and restarting the host dev server, `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`, `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448, and `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
22. Post-parent-notifications-rpcs localhost probes — after applying the linked parent notification helpers, rebuilding, and restarting the host dev server, `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`, `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448, and `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
23. Post-parent-calendar-rls localhost probes — after moving calendar aggregation reads to an RLS-scoped request client, rebuilding, and restarting the host dev server, `GET http://localhost:3000/api/health` returned 200 with `{"status":"ok","db":"ok"}`, `GET http://localhost:3000/api/public/v1/openapi` returned 200 with length 15448, and `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.
24. Post-parent-billing-rls localhost probes — after moving billing aggregation reads to an RLS-scoped request client and fixing the Next proxy alias trace mismatch, the running localhost server returned 200 for `GET http://localhost:3000/api/health` with `{"status":"ok","db":"ok"}`, 200 for `GET http://localhost:3000/api/public/v1/openapi` with length 15448, and 200 for `GET http://localhost:3000/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
25. Post-parent-approve-link-rpc localhost probes — after moving approve-link mutation to `student_review_guardian_link` and applying the pending linked migration stack, the restarted host dev server returned 200 for `GET http://127.0.0.1:3000/api/health` with `{"status":"ok","db":"ok"}`, 200 for `GET http://127.0.0.1:3000/api/public/v1/openapi` with length 15448, and 200 for `GET http://127.0.0.1:3000/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
26. Post-parent-accept-invite-rpc localhost probes — after moving invite acceptance to `parent_accept_invite_code` and applying the linked migration, the restarted host dev server returned 200 for `GET http://127.0.0.1:3000/api/health` with `{"status":"ok","db":"ok"}`, 200 for `GET http://127.0.0.1:3000/api/public/v1/openapi` with length 15448, and 200 for `GET http://127.0.0.1:3000/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
27. Post-parent-link-code-OTP-rpcs localhost probes — after moving OTP challenge persistence and redemption to `parent_request_link_code_otp` and `parent_redeem_link_code_otp`, the running host dev server returned 200 for `GET http://127.0.0.1:3000/api/health` with `{"status":"ok","db":"ok"}`, 200 for `GET http://127.0.0.1:3000/api/public/v1/openapi` with length 15448, and 200 for `GET http://127.0.0.1:3000/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
28. Post-parent-consent-rpcs localhost probes — after moving DPDP consent grant/revoke/list to `parent_record_consent`, `parent_revoke_consent`, and `parent_list_active_consents`, the running host dev server returned 200 for `GET http://127.0.0.1:3000/api/health` with `{"status":"ok","db":"ok"}`, 200 for `GET http://127.0.0.1:3000/api/public/v1/openapi` with length 15448, and 200 for `GET http://127.0.0.1:3000/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
29. Post-parent-messages-rpcs localhost probes — after moving parent message send/list/read-marking to `parent_send_teacher_message`, `parent_list_message_threads`, and `parent_list_thread_messages`, the running host dev server returned 200 for `GET http://127.0.0.1:3000/api/health` with `{"status":"ok","db":"ok"}`, 200 for `GET http://127.0.0.1:3000/api/public/v1/openapi` with length 15448, and 200 for `GET http://127.0.0.1:3000/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
30. Post-XC3-evidence-sync / RCA-11-verifier localhost probes — after synchronizing the RCA-01 release-gate evidence and running the target grade-format verifier, the running host dev server returned 200 for `GET http://127.0.0.1:3000/api/health` with `{"status":"ok","db":"ok"}`, 200 for `GET http://127.0.0.1:3000/api/public/v1/openapi` with length 15448, and 200 for `GET http://127.0.0.1:3000/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
31. Post-full-release-gate localhost probes — after the 2026-07-10 full repo-owned release gate passed `39/39`, the running local server returned 200 for `GET http://127.0.0.1:3000/api/health` with `{"status":"ok","db":"ok"}` and timestamp `2026-07-10T06:20:15.594Z`, 200 for `GET http://127.0.0.1:3000/api/public/v1/openapi` with length 15448, and 200 for `GET http://127.0.0.1:3000/api/feature-flags/check?flag=ff_today_home_v1` with `{"enabled":true}`.
10. `supabase db query --linked -f supabase/migrations/20260710020000_xc3_teacher_join_class_rpc.sql` — exit 0; linked catalog export `artifacts/xc3-teacher-join-class-rpc-live.json` confirms the scoped helper is `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `PUBLIC`.
11. `supabase db query --linked -f supabase/migrations/20260710030000_xc3_parent_report_cache_guardian_rls.sql` — exit 0; linked policy export `artifacts/xc3-parent-report-cache-rls-live.json` confirms `parent_weekly_reports` RLS is enabled with guardian select/insert/update policies using `is_guardian_of(student_id)` and no remaining `parent_weekly_reports_own_*` policies.
12. `supabase db query --linked -f supabase/migrations/20260710040000_xc3_parent_erasure_scoped_rpcs.sql` — exit 0; linked catalog export `artifacts/xc3-parent-erasure-rpcs-live.json` confirms `parent_request_child_erasure` and `parent_cancel_child_erasure` are `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `PUBLIC`.
13. `supabase db query --linked -f supabase/migrations/20260710050000_xc3_school_admin_students_list_rpc.sql` — exit 0; linked catalog export `artifacts/xc3-school-admin-students-list-rpc-live.json` confirms `school_admin_list_students` is `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `public`.
14. `supabase db query --linked -f supabase/migrations/20260710060000_xc3_parent_child_export_scoped_rpc.sql` — exit 0; linked catalog export `artifacts/xc3-parent-child-export-rpc-live.json` confirms `parent_child_export_data` is `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `public`.
15. Post-parent-export localhost probes against the running server on port 3000: `/api/public/v1/openapi` returned 200, `/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`, and `/api/health` returned 200 with `{"status":"ok","db":"ok"}` when retried with a 60-second timeout after an initial 20-second timeout.
16. `supabase db query --linked -f supabase/migrations/20260710070000_xc3_school_admin_student_toggle_rpc.sql` — exit 0; linked catalog export `artifacts/xc3-school-admin-student-toggle-rpc-live.json` confirms `school_admin_toggle_student_active` is `SECURITY DEFINER`, pinned to `search_path=public`, executable by `authenticated`, and not executable by `anon` or `public`.
17. Post-school-admin-toggle localhost probes against the running server on port 3000: `/api/health` returned 200 with `{"status":"ok","db":"ok"}`, `/api/public/v1/openapi` returned 200, and `/api/feature-flags/check?flag=ff_today_home_v1` returned 200 with `{"enabled":true}`.

## Recommended Next Gate

Repo-owned gates are currently green. For reruns after additional changes:

1. `npx tsx scripts/product-readiness-release-gate.ts --list`
2. `npx tsx scripts/product-readiness-release-gate.ts --dry-run`
3. `npx tsx scripts/product-readiness-release-gate.ts`

Environment-owned launch proof remains separate from the local runner and must still be executed before broad launch. Start with `npx tsx scripts/verify-live-readiness-evidence.ts --print-template --release-candidate=<rc-id> --target-environment=<target> --collected-at=<iso-timestamp>`, collect results into that bundle shape, then run `npx tsx scripts/verify-live-readiness-evidence.ts --input=<evidence-bundle.json>`. The bundle must cover: selected certification E2E live run with `CERTIFICATION_RUN_ENABLED=true CERTIFICATION_BASE_URL=<target> CERTIFICATION_RUN_ID=<uuid> npx playwright test e2e/certification`, Edge function deploy/secrets smoke including `supabase secrets set UPSTASH_REDIS_REST_URL=<redacted> UPSTASH_REDIS_REST_TOKEN=<redacted> --project-ref <target-project-ref>` and `parent-portal` invocation/log inspection, live tenant isolation smoke via `LIVE_TENANT_SMOKE_BASE_URL=<target> LIVE_TENANT_SMOKE_PARENT_A_TOKEN=<tenant-a-parent-jwt> LIVE_TENANT_SMOKE_TEACHER_A_TOKEN=<tenant-a-teacher-jwt> LIVE_TENANT_SMOKE_SCHOOL_ADMIN_A_TOKEN=<tenant-a-admin-jwt> LIVE_TENANT_SMOKE_STUDENT_B_ID=<tenant-b-student> LIVE_TENANT_SMOKE_CLASS_B_ID=<tenant-b-class> LIVE_TENANT_SMOKE_SCHOOL_B_ID=<tenant-b-school> npx tsx scripts/verify-live-tenant-isolation-smoke.ts`, live feature-flag DB comparison via `npx tsx scripts/verify-feature-flag-matrix.ts --env=staging` and `--env=production`, target DB AO-10b grade-format verification, target DB RCA-18 grant inspection via `npx tsx scripts/verify-db-function-hardening-live.ts --print-sql` plus `npx tsx scripts/verify-db-function-hardening-live.ts --input=<rows.json>`, live RCA-17 job health inspection via `npx tsx scripts/verify-job-health-live.ts --print-sql` plus `npx tsx scripts/verify-job-health-live.ts --input=<rows.json>`, lower-tier PII-export staff notification and post-deploy export audit review via `docs/runbooks/super-admin-pii-export-notification.md`, production observability check via `npx tsx scripts/verify-incident-id-live.ts --print-sql=<sampled-X-Request-Id>` plus `npx tsx scripts/verify-incident-id-live.ts --input=<evidence.json>`, mobile release validation via `npx tsx scripts/verify-mobile-legacy-traffic-live.ts --print-sql` plus `npx tsx scripts/verify-mobile-legacy-traffic-live.ts --input=<rows.json>`, run `scripts/historical-xp-inflation-quantification.sql` on the target DB and obtain the SLC-1 historical XP clamp/backfill decision, SLC-8 quiz-submit traffic cutover proof, XC-3 service-role exception review / next scoped-RPC narrowing decision, TSB-4 canonical membership live-smoke/repoint execution, and product sign-off on the expanded 13-surface wireframe/CTA matrix plus any remaining surfaced CTA or hidden/dead states product/frontend identifies.
