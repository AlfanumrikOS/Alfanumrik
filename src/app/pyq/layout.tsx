import type { ReactNode } from 'react';

export const metadata = {
  title: 'PYQ Practice | Alfanumrik',
  description: 'Practice previous year questions from CBSE board exams',
};

export default function PYQLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
