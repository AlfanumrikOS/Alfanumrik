# 04 — Prioritised Gap List (business impact first)

Severity uses the brief's scale (P0 data/security/payment/content wrong or blank demo path; P1 a role cannot complete a core job; P2 confusing/inconsistent; P3 polish). Each row names the proof in 01/02/04-matrix.

| # | Sev | Gap | Business impact | Proof |
|---|---|---|---|---|
| G-01 | **P0** | Banned term "tutor" is the live brand line: page `<title>` of the home page, hero copy, login tagline, root metadata, OG image alt, plus a shipped `/tutor` page and `/api/tutor/*` | Every search result, share card and first screen a principal or parent sees positions the product as an "AI tutor", directly contradicting the "learning intelligence" positioning the CEO mandated. | `welcome/layout.tsx:34`, `HeroV3.tsx:371`, `AuthScreen.tsx:359`, `app/layout.tsx:23,47,66,73`; live title "AI Tutor for CBSE Students (Class 6–12) — Alfanumrik"; 135 files contain the word |
| G-02 | **P0** | Foxy, Quiz and the Learn/Exams pages read chapter structure from three different tables and Foxy's own pipeline uses two of them | A chapter can exist for Quiz but not for Foxy (or vice-versa); coverage numbers, "ready" badges and remediation links disagree per surface — the content-mismatch class the brief flags as P0. | 02 §E.4 |
| G-03 | **P0** | Teacher and parent messaging broken in production (RPC EXECUTE grant) | Teachers cannot see parent threads; parents cannot see teacher threads — the one cross-role communication path in the product. | C-001, C-002 |
| G-04 | **P0** | Parent report generation fails 401 and no weekly digest has ever been produced | The parent value proposition on `/for-parents` ("See your child's week, every Sunday") is not deliverable. | C-003; `parent_weekly_reports` 0 rows |
| G-05 | **P0** | Razorpay webhook never observed on the live host | Any payment captured without the client `/verify` round-trip (closed tab, network drop, subscription renewal event) is never reconciled server-side; refunds/`subscription.*` events are never processed. | C-007 |
| G-06 | **P0** | Public site returns 429 to non-browser clients incl. a Googlebot UA; cannot prove Google is exempt | Organic acquisition may be silently zero; robots/sitemap unreachable to crawlers if the challenge applies to them. | C-008 |
| G-07 | P1 | LCP 5–13 s on mid-range Android/4G for every public page; hero is `opacity:0` until JS + IntersectionObserver | Demo-path first impression; fails the 2.5 s budget by 2–5×. | `perf-results.json`; `HeroV3.tsx:357-406` `data-reveal` |
| G-08 | P1 | No global search for any role | Principals/teachers with 30–3,000 students have no way to find a student, class, chapter or question except scrolling filtered lists. | 04-matrix §19 |
| G-09 | P1 | Bulk import is fragmented: CSV+template only at super-admin; school-admin uses JSON without template/preview/error file; teachers JSON-only; parents none | Day-1 school onboarding (the sales promise on `/for-schools`) requires a super-admin to do the school's import. | 04-matrix §3-5, D-8 |
| G-10 | P1 | Two admin consoles, five subscription pages, eight health pages, six RAG pages, three flag surfaces, three student "account" pages, two student homes | Operators and students cannot tell which surface is authoritative; fixes land in one copy. | 01 §1a.4 |
| G-11 | P1 | 62 product API routes and 35 pages are unreachable from any UI; 19 orphan hooks; 13 tombstone Edge Functions still deployed (one still called by mobile) | Dead surface = untested surface with live auth exposure and maintenance cost; mobile users on old APKs hit 410s. | 02 §C, C-006 |
| G-12 | P1 | Student route protection is client-side only; `/super-admin` pages have no server gate | Shells and skeletons flash for unauthenticated visitors; relies on every page's own guard being correct (5 pages render `null` while loading). | `proxy.ts:1277`, `super-admin/layout.tsx:10-27` |
| G-13 | P1 | 169 mutating routes accept unvalidated bodies; 20 admin/teacher mutations write no audit entry; 18 unbounded list endpoints | Malformed input reaches RPCs as `P0001` raises or silent no-ops; teacher actions are invisible in audit. | 02 §E.2/E.3 |
| G-14 | P1 | Curriculum has no editor, no version management, no re-index control; embedding backfill cron is inactive with 21,411 queued rows | Content ops require SQL; RAG drift cannot be corrected from the product. | 04-matrix §6, §15; live `cron.job` |
| G-15 | P1 | No super-admin global announcements; school announcements never used | No way to tell all schools about a change (e.g. exam-season features). | `admin_announcements` unreferenced |
| G-16 | P2 | Six token systems, 9,000+ hex literals, 12 Button/77 Card/42 Modal/47 shell implementations, 9 font families, dark mode disabled by pointing the selector at a never-written attribute | Every new screen adds a variant; contrast and touch-target rules cannot be enforced. | 01 §1b |
| G-17 | P2 | Vernacular: 385 files carry inline `isHi ? … : …` string pairs; no message catalogue; `title_ta/te/bn/mr` columns exist in DB but no UI path | Adding a third language means editing 385 components. | `grep isHi` counts; `curriculum_topics` columns |
| G-18 | P2 | `subscription_plans.price_display` for the top plan still reads ₹1,499 while ₹1,099 is charged | Any future reader (mobile, invoices) shows the wrong price. | live row |
| G-19 | P2 | Anonymous RPC oracles (`is_active_admin`, `get_feature_flag_envelope`); 57 SECURITY DEFINER functions anon-executable; 43 RLS-enabled tables with no policy | Enumeration/feature-leak surface; policy-less tables are deny-all today but brittle. | live probes, advisors |
| G-20 | P2 | 13 Edge Functions log without a correlation id; 11 direct Anthropic calls bypass the retry helper | Incidents like the 5-day key outage are hard to trace; 500/529 cascades recur. | 02 §E.7 |
| G-21 | P2 | Generated types 17 functions behind the live DB (incl. Foxy's retrieval RPC) | Type-unsafe calls in exactly the newest code. | 01 §1d |
| G-22 | P3 | 23 near-identical `error.tsx` files, 72 skeleton variants, 6 page-local marketing navbars, dead `DEPLOY_TARGET`/Dockerfile/ECS config | Maintenance noise. | census-out.txt |

Counts: P0 6 · P1 9 · P2 6 · P3 1 (gap level; defect-level counts are in 03-journeys).
