# Alfanumrik Student-Facing Frontend — Live Inspection Notes

## Server
- Running on localhost:3002, connected to staging Supabase (gzpxqklxwzishrkiaatd.supabase.co)
- Dashboard renders full shimmer skeleton at /dashboard (server SSR → client CSR via ssr:false dynamic import)
- /api/v2/today, /api/quiz/submit, /api/board-score, /api/foxy all return AUTH_REQUIRED without session — auth gating confirmed active

## Auth
- Supabase password-grant sign-in via curl returns email_not_confirmed for admin-created test users
- Admin-created users have user-level email_confirmed_at set but identity email_verified stays false
- App's useAuth() uses localStorage session persistence, not cookies — raw curl auth API can't produce usable session
- Admin API /auth/v1/admin/users/{id}/confirm returns 404 on this project (endpoint not exposed)
- WORKAROUND NEEDED: create user via Supabase JS with service-role, or use magic-link/OTP flow
- Credentials created: audit3@alfanumrik.local / AuditTest2026! (will work once email confirmed)
- Recommendation: use the browser for sign-in (option A), or write a small Node script using @supabase/supabase-js with service-role key to create a confirmed user (option B)

## What was confirmed from live server (no login needed)
1. All auth-gated APIs correctly reject unauthenticated requests with AUTH_REQUIRED
2. Dashboard renders via SSR→CSR hybrid (ssr:false dynamic import in StudentOSDashboard) — correct architecture
3. HTML shells render correctly per route (title "Dashboard | Alfanumrik" at /dashboard)
4. Shimmer skeleton covers auth+student wait on dashboard — correct loading UX
5. Proxy refreshes Supabase session cookies server-side (Layer 1 in proxy.ts) — correct

## What still needs visual confirmation (Step 1 in implementation sequence)
- C1: Foxy embed Stop control — need to open FoxyPanel in embed, send streaming message, confirm no Stop button
- C2: Quiz network-failure provisional results — need to submit quiz with throttled network, confirm "XP pending" vs "+0 XP"
- H2: Foxy Report button in embeds — need to tap Report button on Foxy bubble, confirm no-op
- M2/M3: MasterySnapshot/RevisionRail breakpoints — need to resize dashboard at 768px/1024px boundaries

## Test data created in staging
- audit-test@alfanumrik.local (grade 10, CBSE) — email not confirmed
- audit2@alfanumrik.local (grade 10, CBSE) — email not confirmed  
- audit3@alfanumrik.local (grade 10, CBSE) — email not confirmed, password set
