# 22 — Canonical-vs-Duplicate Decision Register

**Status:** PLANNING ONLY. No execution authorized. Consolidates source-of-truth conflicts found across the original audit and the 2026-08-30 remediation-planning research pass. Each entry needs an explicit decision — none should be resolved by silent engineering judgment, per CEO's "do not create parallel sources of truth" instruction.

**Scope note:** Per CEO authorization, this register does NOT include any decision about dropping `class_students` or `class_enrollments`, migrating vectors, or replacing the active RAG path — those remain explicitly prohibited actions. Entries here are informational/decision-flagging only.

---

## Register

| # | Conflict | Canonical (evidence-supported) | Duplicate/Legacy | Current Divergence | Recommended Decision | Prohibited Actions (per CEO) |
|---|---|---|---|---|---|---|
| 1 | `class_students` vs `class_enrollments` | `class_enrollments` — RLS/RBAC readers already repointed; bidirectional sync trigger active | `class_students` (legacy) | **Zero** — live-verified 2026-08-30, 19/19 pairs matched or both-inactive | Governed by TSB-4 (`scripts/tsb4-canonical-membership-cutover.json`), CEO-gated. No new decision needed here — retirement of the legacy table is a separate, already-tracked go/no-go, not a new source-of-truth question. | **Do not drop `class_students` or `class_enrollments`** (explicit CEO prohibition) |
| 2 | `concept_mastery` write path | `update_learner_state_post_quiz` RPC — atomic, referenced consistently across quiz/diagnostic/dashboard/revision flows | Direct `.upsert()` in `apps/host/src/app/api/tutor/answer/route.ts:247` (Foxy tutor practice-answer path) | **Active** — two live, concurrent write paths; semantics not yet confirmed consistent | Investigate whether `tutor/answer/route.ts`'s upsert counts attempts/correctness identically to the RPC. If not, converge onto the RPC (Foxy path should call the same canonical update, not a parallel upsert). If yes, document why two paths are intentional. | **Do not introduce another mastery system** — converging onto the existing RPC is consolidation, not a new system, and is consistent with this prohibition |
| 3 | RoleName / role type definitions | Unclear — original audit (`02-schema-sot-drift.md`) found 6 conflicting definitions across the codebase (e.g. `packages/lib/src/types.ts` `RoleName` includes `'tutor'` which exists nowhere else in the DB; `packages/lib/src/constants.ts` `UserRole` has `'guardian'` vs `'parent'` split and a `'none'` value not a real DB role) | 6 total conflicting type definitions | Not re-verified in this pass — carried forward from original audit, needs a fresh grep to confirm current state before any consolidation work | Needs a dedicated investigation pass (not done in this round) to enumerate all 6 definitions with exact file/line evidence, determine which (if any) is authoritative, and produce a single canonical `Role` type consumed everywhere. **Not yet packetized — flagging as open for a follow-up round**, since consolidating type definitions used across hundreds of files carries real regression risk and deserves its own dedicated investigation before a fix is proposed. | **Do not introduce another role system** — this is about consolidating existing conflicting type definitions, not creating a new one |
| 4 | Generated TypeScript DB types | Unclear — original audit found generated types file has only 1 consumer, effectively dead | The 1 consumer presumably hand-rolls or duplicates type shapes elsewhere | Not re-verified in this pass | Needs a fresh check: is the generated-types file still being regenerated on schema change (i.e., is it stale)? If dead, either wire it up as the actual canonical source (preferred, since it's schema-derived and can't silently drift) or formally deprecate it — leaving a stale, barely-used generated-types file in the repo is itself a source-of-truth risk (someone may trust it without knowing it's 1-consumer/possibly stale). **Not yet packetized.** | N/A — no destructive action implied either way |
| 5 | RAG chunk stores — vector dimension split | Unclear which is "current" — original audit (DB-18 in FIX-LEDGER) found two RAG chunk stores with incompatible vector geometry (1024 vs 1536 dimensions) | The non-current one | Not re-verified in this pass (would require live schema inspection of both tables) | **Explicitly out of scope for any migration action** per CEO's "do not migrate vectors" and "do not replace the active RAG path" prohibitions. Recommend only a documentation task: confirm which store is actually queried by the live `match_rag_chunks_ncert` RPC (per Packet 3's evidence, this is `rag_content_chunks`, voyage-3, 1024-dim, consistently) and explicitly document the other store's status (dead code to be removed later, or serving a different purpose) — **no schema or data change**, documentation only. | **Do not re-embed or migrate vectors; do not replace the active RAG path** (explicit CEO prohibitions) |
| 6 | `BloomProgression` shape mismatch | Unclear — original audit (`02-schema-sot-drift.md`) flagged a shape mismatch, not otherwise detailed in this round's research | N/A | Not re-verified in this pass | Carried forward as open — needs its own targeted investigation (not done in this round) before a decision can be proposed. | N/A |
| 7 | Feature flags — documented vs. live state | Documentation (runbooks) says 3 constitution-pinned flags are OFF/staged | Live production state: all 3 ON at 100% rollout (`rollout_percentage: NULL`), confirmed 2026-08-30 | **Active, confirmed divergence** — see `21-dependency-graph-and-reclassified-risks.md` §B4 | This is not a "which is canonical" question — production state is ground truth by definition — but it IS a governance question: was the staged-rollout process legitimately bypassed by someone with authority, or did it happen unintentionally? Needs investigation before deciding whether to roll back to staged rollout (which itself carries data-mutation/user-impact risk given likely accumulated usage) or accept 100% rollout as the new intentional state and update documentation to match. | N/A — no schema change implied; this is a process/documentation reconciliation, and any rollback decision needs its own explicit sign-off given real-user impact |
| 8 | Edge Functions — repo source vs. deployed | Repo `supabase/functions/` (49 directories) | Live deployment (102 active functions) | **Confirmed 53-function gap**, including at least one live function (`export-report`) with **no current source of truth in the repo at all**, plus 8+ newer undocumented functions deployed directly (not via CI) — see `21-dependency-graph-and-reclassified-risks.md` §B3 | The repo should be canonical; recover `export-report`'s source from git history (it was deliberately deleted believing "zero live coupling" — that belief is now known to be wrong) and re-establish it under CI control. Audit the newer undocumented functions to determine what each does and who deployed them, before deciding whether to source-control them, retire them, or formally document them as intentionally out-of-repo. | **Do not undeploy Edge Functions** (explicit CEO prohibition) — recovery/documentation only, no removal action without a further explicit decision |

---

## Items Not Yet Fully Investigated (flagged, not resolved)

Two items (RoleName conflicts, generated-types staleness, BloomProgression shape) were carried forward from the original audit without a fresh re-verification pass in this round, given the scope of the six research agents dispatched. These should not be assumed resolved or assumed still-accurate — they need a dedicated follow-up investigation before any decision or packet is written for them. Flagging explicitly here so they are not silently dropped from the remediation-planning scope.
